"""Test-only helpers for architecture enforcement (FND-002).

Everything in this package is test infrastructure: it is never imported by
production code, never ships in the wheel, and exists to drive the positive
and negative architecture fixtures under ``tests/architecture/fixtures/``.
"""
