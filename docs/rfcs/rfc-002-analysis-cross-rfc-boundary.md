# RFC-002 Supporting Analysis：跨 RFC 边界分析（Cross-RFC Boundary Matrix）

> **Status:** SUPPORTING ANALYSIS（边界工件，非 Accepted Decision）
> **服务 RFC：** RFC-002 — Persistence and Transaction Architecture
> **目的：** 精确划定 RFC-002 的**决策所有权**（Owns）与**依赖边界**（later RFC dependency），防止 RFC-002 越权替 RFC-003 ~ RFC-007 做决定。
> **纪律：** 每条标注 `RFC-002 OWNS`（本 RFC 决定）、`INTERFACE for later RFC`（本 RFC 定义契约、后续 RFC 消费）、`DEFERRED to RFC-00X`（本 RFC 不决定、显式移交）、`OUT OF SCOPE`（本 RFC 显式不涉及）。
> **Synchronization Note（2026-08-01）：** 矩阵行 1/2 的 DQ-01 已由用户正式决定（**RFC-002-DQ-01 = ACCEPTED，Accepted with Revision**）：PostgreSQL 是唯一受支持的权威数据库语义；技术栈 = PostgreSQL + SQLAlchemy 2.x synchronous API + Psycopg 3 synchronous driver + Alembic；本地开发与正式持久化测试使用真实 PostgreSQL；SQLite-first REJECTED。矩阵行 3 的 DQ-02 已由用户正式决定（**RFC-002-DQ-02 = ACCEPTED，Accepted with Revision**）：MVP 单一 PostgreSQL 服务；每张业务表唯一所有模块；ORM/Repository/Migration/状态修改 Use Case 模块私有；跨模块仅经 Public Application Contract（读经 Public Query、写经 Public Use Case）；Direct SQL/ORM/Repository 跨模块访问禁止；边界由 Import Linter + AST/Architecture Tests + Repository Ownership Tests + Migration Ownership Conventions + PR 审查强制；每模块独立 PostgreSQL schema 暂缓、物理命名留待实现设计。矩阵行 4 的 DQ-03 已由用户正式决定（**RFC-002-DQ-03 = ACCEPTED，Accepted with Revision**，2026-08-02）：聚合边界 = 业务不变量 + 唯一模块所有权；Atomic Business Commit（DEC-035 六要素）是事务提交协议、非聚合成员资格判据（六要素单事务保持有效）；Task Mega Aggregate REJECTED；默认一 Use Case 一主 Aggregate；跨聚合/跨模块显式协调；UoW 形态移交 DQ-05/06（PROPOSED/PENDING）；Aggregate/Invariant Matrix 实施前必备。以上均为 **Accepted user decision**；矩阵各行所有权归属与理由（研究证据）不变。**Business/Runtime/Checkpoint 三类存储物理划分仍由 DQ-13（PROPOSED/PENDING）拥有，未被 DQ-02 决定。** 矩阵行 5 的 DQ-04 已由用户正式决定（**RFC-002-DQ-04 = ACCEPTED，Accepted with Revision**，2026-08-02）：`domain_version_id` / `version_number` / `revision` 三类明确分离；Domain Version ID 由 Application 层 INSERT 前生成（opaque UUID、不可变）；受保护可变记录使用独立 NOT NULL `revision`，状态修改 Command 携带 `expected_revision`，compare-and-swap 条件更新、零影响行 = 冲突整体回滚；SQLAlchemy `version_id_col` 仅为 Infrastructure 机制；PostgreSQL `xmin` 不作权威业务 revision（Candidate B REJECTED）；SERIALIZABLE 不替代显式 revision（Candidate C REJECTED，仍可作为独立隔离策略讨论）；隔离级别与重试策略仍由 DQ-05/07（PROPOSED/PENDING）拥有；RFC-004 可将 revision 映射 ETag/If-Match（HTTP 协议未决）；正式持久化测试使用真实 PostgreSQL（测试策略归 DQ-16）。矩阵行 6 的 DQ-05 已由用户正式决定（**RFC-002-DQ-05 = ACCEPTED，Accepted with Revision**，2026-08-02）：Business Transaction Owner = Application（Entrypoint / Graph Node 不 begin/commit）；Transactional Application Command = 一个短显式事务 + 一个最终提交点；长流程业务操作 = 多个短事务 + 无事务执行阶段；执行模式 Prepare → Execute Outside Transaction → Commit；四项 PROHIBITED（外部调用持有开放事务、Human Review 跨开放事务、Workflow 暂停跨开放事务、SQLAlchemy Session 跨 Workflow 边界）；Commit-time Revision Revalidation 必选（衔接 DQ-04）；DEC-035 六要素单事务保持有效；External Result Before Commit 非 Current Truth；默认 PostgreSQL 隔离级别 = READ COMMITTED（DQ-04 的默认隔离留白由此决定）；更强隔离 / 锁 / 40001 / 40P01 重试 / SELECT FOR UPDATE / 悲观锁 / 重试上限仍由 DQ-07（PROPOSED/PENDING）拥有；SAVEPOINT 仅为有限 Infrastructure 机制；嵌套业务事务禁止；与外部供应商的分布式事务拒绝；UoW Port 形态仍由 DQ-06（PROPOSED/PENDING）拥有。矩阵行 7 的 DQ-06（UoW Port 形态）已由用户正式决定（**RFC-002-DQ-06 = ACCEPTED，Accepted with Revision**，2026-08-02）：UnitOfWork Port 由 Application 定义、生产实现属于 Infrastructure（可使用 SQLAlchemy）、SQLAlchemy Session 为不暴露给 Domain/Application/Entrypoint/API handler/Graph node/Workflow adapter/Public Contract/外部 Provider adapter 的 Infrastructure 实现细节；每个 Transactional Application Command 创建一个新的**一次性 UnitOfWork**（NEW→ACTIVE→COMMITTED/ROLLED_BACK→CLOSED，commit/rollback/close 后不得重用），对应一个 Session、一个短事务、一个显式业务状态迁移、一个最终 commit 或 rollback 结果；Application Use Case 显式调用 commit()、正常 context-manager 退出不得自动提交；未 commit 退出或异常退出必须 rollback/close/discard（释放连接、UoW 不可用、原始失败在 Application 错误边界保留或翻译）；每个 Transactional Command 最多成功 commit 一次、非法生命周期操作显式失败；UoW 仅暴露显式类型化 Repository Ports，禁止 get_repository(name)/registry/Service Locator/raw Session accessor/通用 execute_sql()/动态 Repository 解析；Repository 不得 begin/commit/rollback/close/begin_nested、不控制 SAVEPOINT 与 UoW 生命周期、不暴露 Session，职责限于加载/暂存/查询/返回项目结果/上抛失败（模块所有权与 DQ-02 一致）；**嵌套业务 UnitOfWork 禁止**，检测企图必须经项目自有错误立即失败；可复用行为提取为 Domain Service / transaction-neutral Application Service / 接收显式 Ports 的内部操作；**Explicit Composite Application Use Case 拥有唯一外层 UoW 与唯一最终提交点**；ACTIVE 期间禁止加入 ambient UoW / 打开第二个业务 UoW / 子操作 commit 作部分提交 / SAVEPOINT 作嵌套业务 commit / commit 权移交 Repository / UoW 存入全局、thread-local 或 Workflow 状态；SAVEPOINT 不是嵌套 UnitOfWork、不由 Application UoW Port 暴露（有限 Infrastructure 级使用受 DQ-05 治理）；UoW Port 默认不暴露 flush()、内部 flush 非业务 commit、flush 失败丢弃当前 UoW；Engine/sessionmaker 长生命周期（Composition Root）vs concrete Session 短生命周期（一个 UoW 一个本地 Command）；全局可变 Session 禁止；scoped_session/thread-local/ContextVar ambient 机制不得作为主要事务所有权或依赖注入机制；并发 Command/Worker 执行/Retry/Rerun/Resume 独立 UoW 与 Session；与 DQ-05 一致（Prepare/Commit 不同 UoW、Execute Outside Transaction 无 UoW、Human Review/Interrupt/backoff 不持有 UoW、UoW 不入 Checkpoint、不在 Resume 恢复）；纯读 Query 独立短 Query Scope（不暴露 commit、不返回 ORM entities）；Candidate B（退出自动提交）与 Candidate C（Repository 管理事务）拒绝；并发/锁/重试归 DQ-07、幂等归 DQ-08、Outbox 归 DQ-09、Event/Audit 归 DQ-10、HTTP 请求作用域归 RFC-004、Workflow/Checkpoint 运行时归 RFC-003、测试分类归 DQ-16。矩阵行 8 的 DQ-07（Concurrency Control）已由用户正式决定（**RFC-002-DQ-07 = ACCEPTED，Accepted with Revision**，2026-08-02，Accepted Direction = Layered Concurrency Control）：项目采用**分层并发控制**而非单一通用锁机制——① 普通业务状态修改（Business Current Truth、Aggregate Roots、Current Truth Pointers、Stage State、Review Package state 等受保护可变记录）默认使用 DQ-04 乐观并发协议（revision + expected_revision + 条件更新 + affected-row 校验），expected_revision 不匹配是语义业务冲突、不得盲目重试；② 命名数据库唯一约束是重复业务事实的最终完整性防线（唯一约束违反不统一重试，错误边界识别命名约束并分类；完整幂等键层级仍归 DQ-08）；③ Durable Execution Guard / Durable Lease + 单调递增 fencing_token 是执行所有权机制（Lease 获取为短 PostgreSQL 事务、长执行前提交、提交后释放行锁/Session/UoW/连接；Worker 不跨 LLM/外部 HTTP 或工具调用/Human Review/Interrupt/backoff/长计算/跨进程执行持有行锁；每次获取/接管/重新分配颁发单调 fencing_token；Worker Commit 最终短事务验证 expected_revision + Lease Holder + fencing_token + Attempt/Run identity + 后续 DQ-08 幂等身份 + 业务不变量；持旧 token 的过期 Worker 不得提交；进程本地 asyncio.Lock/threading.Lock/mutex 仅非权威优化，正确性须在进程重启/崩溃/多进程/多副本/机器替换/内存丢失下保持）；④ SELECT FOR UPDATE SKIP LOCKED 仅限显式队列式 Claim 短事务（select → lock → assign durable holder/Lease/fencing token → commit → release → execute outside transaction），不得用于普通 Current Truth 读取/Human Review 读取/完整结果集查询/绕过 expected_revision 冲突/静默忽略正在修改对象/跨外部调用持有执行所有权；悲观行锁（FOR UPDATE/NOWAIT）非全局默认（采用须完整记录 + 确定性全局锁顺序）；Session-level Advisory Locks 禁止为默认或权威机制、Transaction-level Advisory Locks 非默认（仅自然行无法表达并发范围时经独立架构审查）；SQLSTATE 40001/40P01 归类为可能瞬时的数据库事务失败，有限自动重试由 Application Transaction Runner / Command Executor 拥有（Repository/Session/UoW 不得静默自重试循环），每次重试 = 全新一次性 UoW + 全新 Session + 重新加载状态 + 重新评估前置条件 + 重新运行 revision 与 Lease 验证 + 丢弃失败尝试 ORM entities，默认预算 1 次初始 + 最多 2 次重试 = 共计 3 次尝试，无限/无界重试禁止，backoff/jitter 在事务外（参数/指标/阈值归 RFC-007，有界要求不得移除）；语义冲突（stale revision/fencing token/Lease/review、业务不变量拒绝、过期外部结果、未分类 unique_violation、未定义策略 NOWAIT）不盲目重试；外部 LLM/HTTP/工具执行不入事务重试循环（Commit 可用不可变外部结果重试但不重调 Provider）；五类并发场景控制组合映射（duplicate resume = Lease + fencing_token + DQ-08 幂等身份；concurrent approval = expected_revision CAS + 唯一 Review Decision identity；stale worker = Lease Holder + fencing_token + expected_revision；repeated command = 命名唯一约束 + DQ-08 幂等记录；simultaneous invalidation = 所属 Aggregate/Stage State/Current Truth Pointer 的 expected_revision CAS）；LangGraph thread_id 与 Checkpoint identity 仅定位工作流状态与恢复位置，不得视为 Business Concurrency Lock / Durable Lease / fencing_token / 业务 Idempotency Record / 单一活动 Resume 证明；**Concurrency Scenario Matrix（13 字段）与真实 PostgreSQL 多 Worker Concurrency Technical Spike（9 项验证）均为实现前置条件（REQUIRED），但均不由 DQ-07 接受授权**（Matrix 需后续规划或实现就绪授权；Spike Issue/Branch/PR/代码/测试/基础设施创建需单独明确用户授权）；剩余所有权：完整幂等键层级与响应重放 → DQ-08、Outbox/Dispatch Claim 实现 → DQ-09、Event/Audit 持久化顺序 → DQ-10、Checkpoint 并发与 Runtime 对账 → RFC-003、API 冲突状态码或 ETag/If-Match 协议 → RFC-004、完整持久化测试分类与 CI 执行设计 → DQ-16、运维重试指标与阈值 → RFC-007；详细持久化测试组织归 DQ-16，所有正式并发语义测试使用真实 PostgreSQL。**官方能力与项目决定的区分（DQ-07）：** PostgreSQL/SQLAlchemy 官方能力为 FOR UPDATE / SKIP LOCKED / NOWAIT 悲观行锁、Advisory Locks、SQLSTATE 40001/40P01、`with_for_update`/`version_id_col`；LangGraph OSS 官方事实为无同一 thread_id 并发防护；**Durable Lease + 单调 fencing_token 是 Accepted Architecture Decision（项目用户决定），非 PostgreSQL 或 LangGraph 官方强制架构**；完整幂等/Outbox/Event-Audit/Checkpoint 对账/API 冲突协议/测试分类仍为留白（Deferred decision）。矩阵行 9 的 DQ-08（Idempotency Model）已由用户正式决定（**RFC-002-DQ-08 = ACCEPTED，Accepted with Major Revision**，2026-08-02，Primary Direction = Candidate B，Supporting Principle = Candidate C，Rejected Direction = Candidate A as Universal Table）：项目采用**分层幂等模型**——各幂等层由相应 Owning Module 分层持久化（模块私有、与自身业务更新同事务提交、遵守 DQ-02 表所有权），所有幂等层共享统一概念与行为契约（logical operation identity / owning module / idempotency scope / idempotency key / input fingerprint / execution status / retry-rerun semantics / unique constraint / result replay semantics / atomic transaction boundary；**统一概念契约不意味着统一物理表**），Candidate A 作为跨模块、跨 Command/Workflow/Consumer/Dispatch/Provider 调用的万能 Idempotency Table 被拒绝；Candidate B（分层各自存储）接受为主要持久化方向，Candidate C（天然幂等 set/ensure/replace/compare-and-set 语义）接受为强制设计原则（不替代显式记录、唯一约束或执行所有权控制）；七类身份（Command ID / Idempotency Key / Attempt ID / Stage Run ID / Review Decision ID / Dispatch ID / Provider Call Identity）明确区分不得混用；Retry 复用同一 Command ID/Idempotency Key/Stage Run ID/Input Fingerprint + 新 Attempt ID（不创建新 Domain Version），Intentional Rerun 创建新逻辑身份 + rerun_of；Versioned Input Fingerprint（规范化业务输入 + 版本化规则）；同 Scope+Key+相同 Fingerprint 重放原 Application Result、不同 Fingerprint 返回 Idempotency Key Conflict；幂等成功记录与 Business Current Truth 同一 DEC-035 Atomic Business Commit 提交；Consumer Dedup Marker 与消费业务更新同事务；Workflow Resume 经 Command Identity + 数据库幂等记录 + Lease 与 Fencing 校验（Checkpoint 不替代业务幂等记录）；IN_PROGRESS 执行所有权与 DQ-07 Durable Lease / Lease Holder / Attempt ID / fencing_token 协同（旧 Worker 不得写入 SUCCEEDED）；瞬时失败不永久固化、确定性终局语义结果可稳定重放；Provider Retry 复用同一 Provider Call Identity（DB 事务重试不重调已完成 Provider 调用；无原生幂等则 Durable Call Ledger）；**Idempotency Identity Matrix（18 字段）为幂等实现前置条件（REQUIRED），但不被 DQ-08 接受授权**（不新增独立 Spike；DQ-07 的真实 PostgreSQL 多 Worker Concurrency Technical Spike 覆盖幂等并发场景）。**官方能力与项目决定的区分（续，DQ-08）：** Idempotent Consumer/Receiver（去重须与业务同事务）与 Stripe 幂等键（首个响应原样重放、客户端键、唯一约束、参数比对）是官方/权威模式能力；PostgreSQL 命名唯一约束是官方能力；**分层模块私有幂等存储 + 统一语义契约（非统一物理表）、七类身份模型与 Retry/Rerun 语义、Versioned Input Fingerprint 规则、状态机表达、Provider Call Ledger 要求与天然幂等设计原则是 Accepted Architecture Decision（项目用户决定），非 Stripe、AWS 或 PostgreSQL 官方强制架构**；Outbox/Dispatch 的 Relay 与 Worker Backend（→RFC-003）、Event/Audit 已由 DQ-10 决定（2026-08-02：六类记录独立语义 + append-only Audit 同事务 + 独立 Transactional Integration Event Outbox）、物理存储（→DQ-13）、Retention（→DQ-15）、HTTP 幂等协议（→RFC-004）、测试分类（→DQ-16）、安全/PII（→DQ-17）仍为留白（Deferred decision）。矩阵行 10 的 DQ-09（Transactional Outbox / Durable Dispatch）已由用户正式决定（**RFC-002-DQ-09 = ACCEPTED，Accepted with Major Revision**，2026-08-02，Accepted Candidate = Candidate B，Formal Pattern = PostgreSQL-backed Transactional Durable Work Intent）：MVP 采用 PostgreSQL-backed Transactional Durable Work Intent 作为内部可靠工作调度模型；Durable Work Intent 表示「系统必须执行的工作」，不得与 Domain/Application/Integration Event、Audit Record、State Transition Record、Workflow Checkpoint、Business Current Truth、通用 Message Envelope 混用；Candidate A 不作 MVP 内部任务模型（保留为未来 Integration Event Outbox 方向，语义独立、不默认共用业务身份/状态机/Payload/Retention/Consumer Protocol/Completion Semantics/Retry Policy）；Candidate C 独立 Broker 不在 MVP 引入（未来仅作 Delivery Backend / Relay Target / Worker Transport / 扩展性基础设施，不得替代事务内 Durable Intent）；Dispatch Capability 为明确且唯一的 Owning Module（拥有 Work Intent Repository Port / Infrastructure Repository / ORM Model / Migration / Claim / Completion / Retry-Cancel-Supersede Application Contract；业务模块不直接访问 Dispatch ORM/表/Session/Repository；跨模块创建经 Public Application Contract / Composite Application Use Case / 同一顶层事务编排；不绕过 DQ-02 边界）；业务状态更新与 Work Intent 同一 PostgreSQL Atomic Business Commit 提交（Intent 写入失败整体回滚、回滚不留可领取 Intent、不先提交业务再非原子补写）；API 仅在 Intent 持久化 Commit 后返回 accepted（accepted 仅表示工作已可靠记录，不表示 Worker/Provider/Workflow/Current Truth 完成；HTTP/Polling API 协议留 RFC-004）；禁止 asyncio.create_task / 临时 Background Task / 进程内 Queue / 内存 Flag / 非持久化 Callback / Fire-and-forget coroutine / 仅 LISTEN-NOTIFY / 仅 Broker Publish / 仅 LangGraph Runtime 内存状态作唯一可靠调度保证（仅可作性能或唤醒优化）；dispatch_id 稳定唯一，Work Execution Retry 同 dispatch_id + 新 delivery_attempt_id（或 DQ-08 一致 Attempt Identity），Attempt Identity 不替代 dispatch_id，Intentional Rerun 新 dispatch_id + rerun_of / parent_dispatch_id，由明确 Application Intent 区分；稳定 Envelope（dispatch_id / intent_type / owning business operation / target business scope / command_id / stage_run_id（如适用）/ Input Fingerprint / base domain_version_id（如适用）/ expected_revision（如适用）/ immutable payload 或 payload reference / ordering key（如适用）/ created_at / available_at / priority（如适用））；生命周期至少 PENDING-AVAILABLE / LEASED-IN_PROGRESS / SUCCEEDED / FAILED_RETRYABLE / FAILED_TERMINAL / CANCELLED / SUPERSEDED（不以删除表达状态变更）；Claim = 短 SELECT FOR UPDATE SKIP LOCKED 事务（分配 Lease Holder + 单调 fencing_token + Attempt Identity，提交后释放行锁/Session/UoW/连接；仅限队列式 Claim）；长执行在数据库事务之外（LLM/HTTP/工具/Human Review/Interrupt/Backoff/跨进程等待不持行锁或 Session）；最终提交在新短事务中重新验证 dispatch_id / Lease Holder / fencing_token / Attempt Identity / command_id / Input Fingerprint / expected_revision / 适用幂等身份 / 业务不变量（旧 Worker 或过期 Lease 不得完成 Intent / 写 SUCCEEDED / 提交 Current Truth / 覆盖新 Worker 结果 / 重绑新 Domain Version）；Completion 与业务结果需立即一致时九要素同一最终短事务；Provider 已成功 + 最终 DB Commit 失败 = Retry 保持同一 Provider Call Identity、不自动重调、经原生 Idempotency / Durable Call Ledger / 结果查询 / 对账恢复，不解释为分布式 Exactly-once Transaction；Delivery = AT-LEAST-ONCE（不承诺 EXACTLY-ONCE DELIVERY；唯一业务效果 = DQ-08 幂等 + Consumer Dedup + 命名唯一约束 + DQ-07 Lease/Fencing + revision + Atomic Commit；所有 Worker 和消费者必须安全处理重复 Delivery）；Database Transaction Retry（仅 40001/40P01、新 UoW/Session、默认最多三次总事务尝试、不重调 Provider、不创建新逻辑 Dispatch）与 Work Execution Retry（同 dispatch_id + 新 Attempt Identity + 重新 Lease + 新 fencing_token + 重新验证）分离，DQ-07「三次事务尝试」不直接作 Work Execution 最大次数（Work Retry 策略留 RFC-003/RFC-007）；默认不承诺全局严格顺序（局部顺序按具名业务 Scope，不用单一全局队列锁，Ordering 实现与调度算法留 RFC-003）；周期性 PostgreSQL Polling 是权威恢复路径，LISTEN/NOTIFY 或进程内 Signal 仅非权威 Wake-up 优化（不作 Durable Dispatch Record / Delivery Acknowledgement / 唯一恢复依据 / Exactly-once Guarantee；Wake-up 与 Claim 分离）；**Relay 与 Worker Backend 具体实现（direct PostgreSQL polling / LISTEN-NOTIFY acceleration / broker relay / worker lifecycle / deployment topology / runtime reconciliation / crash recovery）留 RFC-003**；DQ-09 不决定 DQ-10 事件/审计语义（DQ-10 确认需要 Integration Event 持久化时用语义独立 Event Outbox，不把 Work Intent 当事件历史）；Retention/Archive/Payload Cleanup/物理删除留 DQ-15（前不依赖快速物理删除），Payload Encryption/Redaction/Secret/PII 留 DQ-17，测试分类与 CI 留 DQ-16；DQ-09 不新增独立 Matrix——现有 Idempotency Identity Matrix 与 Concurrency Scenario Matrix 须在后续获得授权时补充 Dispatch 场景（Dispatch ID / Delivery Attempt Identity / Consumer Scope / Provider Call Identity / Retry Identity / Rerun Identity；Claim Race / Lease Expiry / Worker Crash / stale fencing_token / duplicate Delivery / Cancel-versus-complete / simultaneous retry / ordering conflict），**Matrix 创建或修改 REQUIRED 但 NOT AUTHORIZED**；DQ-07 真实 PostgreSQL 多 Worker Concurrency Technical Spike 继续有效并覆盖 concurrent Claim / SKIP LOCKED / Lease Takeover / stale Worker Completion / duplicate Delivery / Worker Crash Recovery / Polling Recovery / Cancel-versus-complete / zero partial business writes（Spike 仍需单独明确授权）；所有正式 Durable Dispatch 语义验证使用真实 PostgreSQL（15 项最低覆盖见 DQ-09 第 101 点）。**官方能力与项目决定的区分（续，DQ-09）：** Transactional Outbox（同库事务写 outbox + 独立 relay、at-least-once → 消费端必幂等）、Guaranteed Delivery（store-and-forward）、relay = Polling vs Log Tailing 是**官方/权威模式能力**（模式定义研究）；PostgreSQL SELECT FOR UPDATE SKIP LOCKED 与 LISTEN/NOTIFY 是官方能力（**LISTEN/NOTIFY 是官方异步通知机制，不是可靠队列或 Delivery Guarantee**）；**PostgreSQL-backed Transactional Durable Work Intent 作为 MVP 内部可靠工作调度模型及其全部语义（Work Intent 与 Event Outbox 语义分离、Dispatch Capability 所有权、accepted 边界、Dispatch Identity、Envelope、生命周期、Claim/Lease/Fencing、Completion Atomicity、at-least-once 与唯一业务效果组合保证、两层 Retry 分离、Polling 权威恢复路径、LISTEN/NOTIFY 非权威唤醒、Matrix 扩展要求）是 Accepted Architecture Decision（项目用户决定），非 PostgreSQL 官方强制架构，亦未将 LISTEN/NOTIFY 描述为可靠队列或 Delivery Guarantee**；Event 分类与审计语义已由 DQ-10 决定（2026-08-02：六类 Event/Record 独立语义 + append-only Audit 同事务 + 独立 Transactional Integration Event Outbox + 非 Event Sourcing），Relay/Worker Backend 与部署拓扑（→RFC-003）、Work Retry 运维参数（→RFC-007）、Retention（→DQ-15）、Security（→DQ-17）、测试分类（→DQ-16）仍为留白（Deferred decision）。矩阵行 11 的 DQ-10（Event & Audit Persistence）已由用户正式决定（**RFC-002-DQ-10 = ACCEPTED，Accepted with Major Revision**，2026-08-02，Accepted Candidate = Candidate A；Candidate B = REJECTED AS UNIVERSAL EVENT / AUDIT TABLE；Candidate C = REJECTED AS PROJECT-WIDE ARCHITECTURE）：Domain Event / Audit Record / State Transition Record / Application Event / Integration Event / Observability Event 六类记录语义独立，不使用 Universal Event Table；Audit Record 是 append-only 权威问责证据，与对应 Business Current Truth 修改同一 DEC-035 Atomic Business Commit（写入失败整体回滚、回滚不留成功 Audit、不异步补写、不 UPDATE 覆盖，更正仅追加 Correction / Superseding / Reversal Record 且引用被更正记录，Invalidation / Supersession / Reversal 不等于物理删除）；State Transition Record 是显式类型 Audit Record（可物理共用 Audit Ledger——明确 record_type / Typed Payload Schema / Schema Version / Transition-specific Constraints / 可验证 from/to state——物理共用不代表语义合并，不充当 Integration Event 或 Business Current Truth）；Domain Event 默认不自动持久化，Commit 前 Handler 在最外层 UoW 内 Commit 前执行（不嵌套 UoW / 不独立 Commit / 不调用外部 Provider），映射由明确 Application Policy 决定、不由 ORM Hook / 数据库触发器隐式推断；Application Event 仅 Commit 后本地 LOCAL / BEST-EFFORT / NON-DURABLE 通知（必须执行工作用 DQ-09 Durable Work Intent；Handler 失败不回滚已提交业务）；Integration Event 可靠发布使用独立 Transactional Integration Event Outbox（Outbox Record 与业务状态 + 必要 Domain Version + Current Truth Pointer + Audit Record + State Transition Record（如适用）+ Idempotency Result 同一 PostgreSQL Atomic Business Commit；Delivery = AT-LEAST-ONCE，不承诺 EXACTLY-ONCE DELIVERY；Consumer 依 Event Identity + Consumer Scope 去重且 Dedup Marker 与消费业务更新同事务；`source` + `event_id` 唯一识别逻辑事件；Outbox 与 DQ-09 Durable Work Intent 不共用 Identity / State Machine / Payload Schema / Completion State / Retry Policy / Retention Policy / Consumer Protocol）；CloudEvents-compatible Envelope 仅可选互操作方向（只解决事件格式和互操作元数据，不替代 Transactional Outbox / Atomic Commit / Relay / Retry / Consumer Dedup / Delivery Guarantee；版本 / 字段映射 / 协议选择留 RFC-003 或独立 Integration Design）；Observability Event 属 RFC-007 非权威 Telemetry（不是 Business Current Truth / Audit Record / State Transition Record / Idempotency Record / Durable Work Intent / Integration Event；失败不回滚业务、不替代 Audit、不作业务成功唯一证据）；Audit Ledger / State Transition Record / Integration Event Outbox 不构成 Business Current Truth，不引入 Event Sourcing（Snapshot / 历史留 DQ-11）；所有权：Domain Event Definition 归所属 Business Module、Audit Ledger 归唯一 Audit Capability、Integration Event Outbox 归唯一 Integration Event Capability、Observability Pipeline 归 RFC-007，业务模块经类型化 Application Port / Public Application Contract 创建记录，ORM Hook / SQLAlchemy Event Listener / 数据库 Trigger 不隐式决定 Event / Audit 生成与语义；Event Relay / Broker / Polling / 发布状态机 / Dead-letter / 部署拓扑留 RFC-003；Retention 留 DQ-15（前不假设短期删除）、测试留 DQ-16、Security 留 DQ-17；**Event & Record Classification Table（16 列）为持久化实现授权前 Architecture Readiness Package 必备——REQUIRED 但 NOT AUTHORIZED**；不新增独立 Matrix 或 Technical Spike（DQ-07 真实 PostgreSQL 多 Worker Spike 继续有效并覆盖 Integration Event 重复 Delivery / Consumer Dedup / Relay Crash / stale Event Publish Attempt / 无部分业务写入）；不得借 DQ-10 归档任务创建 Audit Schema / Event Schema / Event Registry。**官方能力与项目决定的区分（续，DQ-10）：** Fowler Audit Log（append-only、问责）与 Audit Log ≠ Domain Event 分界、Fowler 四类事件（Notification / State Transfer / ES / CQRS 不可混淆）、Transactional Outbox / Idempotent Consumer 模式是**官方/权威模式能力**；CloudEvents 是 CNCF 官方事件格式标准；**六类 Event/Record 独立语义、Audit Record 同 DEC-035 事务与追加式更正模型、State Transition Record 物理共用 Audit Ledger、Application Event Commit 后 best-effort、独立 Transactional Integration Event Outbox 与 at-least-once、Consumer Dedup 基于 Event Identity + Consumer Scope、Outbox 与 Work Intent 语义分离、CloudEvents-compatible Envelope 作可选互操作方向（不替代 Outbox / Dedup / Delivery Guarantee）、Observability Event 非权威边界、不构成 Business Current Truth、不引入 Event Sourcing、Audit Capability / Integration Event Capability 所有权、Application 显式 Event Translation、Classification Table 实现前置均为 Accepted Architecture Decision（项目用户决定），非 Fowler / CNCF CloudEvents / PostgreSQL 官方强制架构，未将 CloudEvents 描述为 Outbox / Durable Delivery / Transaction Protocol / Dedup / Exactly-once Guarantee**；Retention / Security / 测试分类 / Relay-Broker-发布状态机 / 部署拓扑 / CloudEvents 版本映射仍为留白（Deferred decision，→ DQ-15/16/17/RFC-003 或独立 Integration Design）。矩阵行 12 的 DQ-11 已由用户正式决定（**RFC-002-DQ-11 = ACCEPTED，Accepted with Major Revision**，2026-08-02）：Accepted Candidate = Candidate A；Formal Model = **Authoritative Current Truth + Immutable Business Version Snapshots + Append-only Audit / State Transition History + Optional Derived Query Projections**；PostgreSQL Business Current Truth 是唯一业务权威来源，Current Truth = Logical Business Object + Explicit Current Truth Pointer + Selected Immutable Business Version；Audit Record / State Transition Record / Domain Event / Integration Event / Workflow Checkpoint / Observability Event / Query Projection 均不构成 Business Current Truth；**Candidate B 完整 Event Sourcing 拒绝（FULL EVENT SOURCING CONFLICTS WITH DEC-013）、Candidate C 仅当前状态覆盖拒绝（CURRENT-STATE-ONLY OVERWRITE CONFLICTS WITH DEC-024）、Delta-only Version History 拒绝作为权威 MVP 历史模型**；系统不得重放 Audit / State Transition / Domain / Application / Integration / Observability Event / Durable Work Intent / Workflow Checkpoint 重建权威 Current Truth。正式 Business Version 逻辑完整、不可变、独立可读（不依赖 Event/Delta 重放、不经可变 Current Pointer 解释历史）；仅成功 Atomic Business Commit 产生正式 Domain Version（DB/Work Retry、Lease、Checkpoint、回滚事务、未提交 LLM 临时结果不得创建）；Current Truth 由带独立 revision 的显式 Current Truth Pointer 经 CAS 选择（`MAX(version_number)` 禁止）；Invalidation 不删除且不静默回退；**Restore 为新前向 Business Command（非数据库 Rollback）**；Query Projection 派生非权威；不引入通用 Bitemporal Model / SQL `AS OF`；五种 Snapshot 术语独立。**官方能力与项目决定的区分（续，DQ-11）：** Fowler Event Sourcing 分界、Martin Fowler 版本化/不可变快照与 bitemporal 概念、PostgreSQL MVCC / Backup / PITR / Temporal 扩展 = **官方能力 / 权威模式**；**Authoritative Current Truth + Immutable Business Version Snapshots + Append-only Audit/Transition History + Optional Derived Query Projections 的组合模型、逻辑完整快照、显式 Current Truth Pointer、Invalidation 不删除且不静默回退、Restore 前向命令、Query Projection 非权威、仅成功 Atomic Business Commit 产生版本、不引入通用 Bitemporal Model / SQL AS-OF、五种 Snapshot 术语独立、Aggregate / Invariant Matrix 扩展列与 DQ-07 Spike 扩展均为 Accepted Architecture Decision（项目用户决定），非 Fowler / PostgreSQL / 任何第三方官方强制架构**；**不得把 PostgreSQL Backup / PITR、Temporal Feature 或数据库 MVCC 描述成 Business Version History，不得把 Event-Sourcing Snapshot 描述成当前项目正式 Business Snapshot**；历史读取 API 契约移交 RFC-004，Source/Evidence 边界、Schema 演进、Retention、Security、测试分类仍为留白（Deferred decision，→ DQ-12/14/15/16/17）。以上均为 **Accepted user decision**；矩阵各行所有权归属与理由（研究证据）不变。矩阵行 1/2（DQ-01）、行 3（DQ-02）、行 4（DQ-03）、行 5（DQ-04）、行 6（DQ-05）、行 7（DQ-06）、行 8（DQ-07）、行 9（DQ-08）、行 10（DQ-09）、行 11（DQ-10）、行 12（DQ-11）与行 13（DQ-12）与行 14（DQ-13）与行 15（DQ-14）自此为 ACCEPTED；DQ-15~DQ-17 仍 PENDING。**官方能力与项目决定的区分（续，DQ-14，2026-08-03）：** Alembic autogenerate/forward-only/batch/offline SQL、PostgreSQL 快速加列/`CREATE INDEX CONCURRENTLY`/`NOT VALID`+`VALIDATE`/DDL 事务性（除 CONCURRENTLY）= 官方能力；**Forward-recovery-first/Single Business Head/Migration History Immutability/Expand-Migrate-Contract/Resumable Backfill/Reversibility Classification/Destructive-Non-transactional Gate/Technical≠Business Backfill/Business Alembic≠Vendor Migration/Schema Version Identity Separation/Schema Migration Compatibility & Risk Table/Rollout Spike = Accepted Architecture Decision（项目用户决定），非 Alembic/PostgreSQL 官方强制架构**；不得声称 Autogenerate 天然安全/所有 Revision 可无损 Downgrade/`alembic check` 是安全证明/Alembic Head 表 Backfill 完成/Business Alembic 管 Vendor Tables/所有 DDL 完整事务性/CONCURRENTLY 可入普通事务/DQ-14 已授权创建 Migration；完整测试→DQ-16、Retention→DQ-15、Security→DQ-17、Vendor Migration→RFC-003、Retrieval Index Rebuild→RFC-005。**官方能力与项目决定的区分（续，DQ-13，2026-08-02）：** LangGraph Checkpointer（PostgresSaver 4 表 + setup/checkpoint_id/delete_thread/durability=sync-async-exit/无内建 TTL/OSS 无并发防护）、PostgreSQL Schema/Role/Pool/search_path、Psycopg Pool 与 Pooler（Transaction/Session Mode）= 官方能力；**Shared PostgreSQL Service + Isolated Checkpoint Plane、三个持久化平面分离、Dedicated Schema 必须验证（不假设 PostgresSaver 原生支持）、Dedicated Database Fallback、Workflow Execution Registry、Reconciliation 八状态模型、Whole-thread Deletion、Workflow Checkpoint Boundary & Reconciliation Table、Workflow Checkpoint Isolation & Reconciliation Spike = Accepted Architecture Decision（项目用户决定），非 LangGraph/PostgreSQL/Psycopg 官方强制架构**；不得声称 PostgresSaver 已被证明原生支持 Dedicated Schema、Checkpoint 与 Business Commit 是同一事务、Checkpoint 提供并发锁、thread_id 是业务身份、Checkpoint Time Travel 是 Business Restore、Partial Pruning 已被安全验证；生产 Checkpointer 选型/durability/serde/并发防护→RFC-003、Retention→DQ-15、Serializer 安全→DQ-17、测试→DQ-16。**官方能力与项目决定的区分（续，DQ-12，2026-08-02）：** PostgreSQL TOAST / bytea / Large Object、对象存储 ETag / Conditional Put / Multipart Checksum、W3C PROV 模型 = 官方能力/公开标准；**Source/Evidence Graph、Content-addressed Object Key、Upload/Verify/Finalize 协议、SourceVersion/Fragment/EvidenceLink 模型、Storage Classification Table、External Object Consistency Spike = Accepted Architecture Decision（项目用户决定），非官方强制架构**；不得把 TOAST 描述成业务 Storage Policy、不得把 Large Object 描述成默认方案、不得把 ETag 描述成项目权威 Content Hash、不得把 Retrieval Index 描述成 Business Current Truth、不得把 Search Result 描述成正式 Evidence；Retention → DQ-15、Security → DQ-17、测试 → DQ-16。行 16（DQ-15）自此为 ACCEPTED；DQ-16~DQ-17 仍 PENDING。**官方能力与项目决定的区分（续，DQ-15，2026-08-03）：** PostgreSQL `DELETE`/VACUUM/物理空间回收、事务性 DDL、Backup/PITR 与 Restore 机制、对象存储 Lifecycle/Versioning/Object Lock 能力、LangGraph Checkpointer `delete_thread`/应用层 cron 清理建议 = 官方能力；Classified Retention & Disposition Policies、Purpose/Legal-basis-driven Retention Clocks、Reference-aware Deletion Eligibility、Hold Overrides、Normal-lifecycle Immutability + Governed Exceptional Erasure/Redaction Paths、Idempotent Auditable Purge Orchestration、Separate Storage-plane Lifecycles、Central Governance + Decentralized Ownership、受治理 Deletion/Erasure Case、Deletion State Machine、最小 Tombstone/Deletion Proof、Data Retention, Hold & Deletion Policy Table、Retention & Deletion Safety Technical Spike = Accepted Architecture Decision（项目用户决定），非 PostgreSQL、LangGraph 或任何法规的官方强制架构。不得声称某一法律要求所有数据立即删除、所有 Business History 在任何法律情况下都绝对不可处置、Soft Delete 等于 Physical Purge、Archive 等于 Delete、SQL DELETE Commit 表示底层字节立即消失、Primary Purge 表示 Backup 已完成处置、Backup Restore 可以不重放 Deletion Ledger 即重新暴露已删除数据、Deletion Proof 可以保存完整原敏感载荷、DQ-15 已决定具体保留期限、DQ-15 已授权执行数据删除。具体保留期限与法律例外机制 → 未来业务/合规决定 + DQ-17（延迟法律/合规决定）；Backup/PITR 周期与基础设施、Purge 实现 → 后续独立决定（延迟实现决定）；测试分类 → DQ-16。行 17（DQ-16）自此为 ACCEPTED；DQ-17 仍 PENDING。**官方能力与项目决定的区分（续，DQ-16，2026-08-03）：** PostgreSQL MVCC / 40001 / 40P01 / SKIP LOCKED / SAVEPOINT / DDL 事务性、SQLite 全库单写者、SQLAlchemy sync/async stack、metadata.create_all() 与 Alembic Migration 区别 = 官方能力与官方事实；Layered Test Strategy、SQLite 非权威边界（Persistence Acceptance Engine 禁止）、真实 PostgreSQL 验收范围、Fake Pass ≠ PostgreSQL Adapter Pass、Sync-only MVP 栈、PR Required Tier、禁止 Retry-to-green、Persistence Test Coverage & Fidelity Table、Common Persistence Test Harness Qualification = Accepted Architecture Decision（项目用户决定），非 PostgreSQL、SQLite、SQLAlchemy 或 Alembic 官方强制架构。不得声称 SQLite 能证明 PostgreSQL 并发语义、SQLite Test 是正式 Persistence Acceptance、Fake Pass 替代 PostgreSQL Adapter Pass、所有测试必须使用 PostgreSQL、Async Persistence Stack 已被接受、metadata.create_all() 等于 Migration Acceptance、自动 Retry 可以解决 Flaky Test、DQ-16 已授权创建测试。Common Test Harness 形态 / CI 拓扑 / Fixture 策略 → 第一个获授权 Persistence Spike（延迟实现决定）；Async Stack 兼容性 → 未来新架构决定；精确 Sensitive Test Fixture 规则 → DQ-17。行 18（DQ-17）自此为 ACCEPTED；全部 17 个 Decision Question 已决定（Pending = 0）；RFC-002 已于 2026-08-04 由用户正式接受（RFC-002 = ACCEPTED；Acceptance ≠ Authorization，Implementation = NOT AUTHORIZED）。**官方能力与项目决定的区分（续，DQ-17，2026-08-03）：** LangGraph RCE 风险/strict msgpack/Secret 明文序列化/EncryptedSerializer、PostgreSQL Role/Grant/RLS/search_path/BYPASSRLS/Pool 机制 = 官方能力与官方事实；Secret-reference-only Persistence、SECRET/PII/SENSITIVE BUSINESS DATA 分类语义、Confidentiality Level + Handling Tags、Role/Pool 拆分清单、CROSS-TENANT/CROSS-SECURITY-DOMAIN DEDUP = PROHIBITED FOR MVP、Sensitive Data, Secret & Cryptographic Control Matrix、Cross-cutting Persistence Security Qualification、Conditional Encryption Spike = Accepted Architecture Decision（项目用户决定），非 LangGraph、PostgreSQL、OWASP 或其他外部标准原生强制实现。不得声称 SecretStr 能保证不持久化、Encrypted Checkpoint 允许保存 Secret、Checkpoint 所有 Metadata 都自动加密、Infrastructure At-rest Encryption 足以替代授权、所有字段必须 Field-level Encryption、RLS 替代 Application Authorization、Hash 等于 Anonymization、Secret 从最新 Commit 删除后无需轮换、DQ-17 已实现 Secret Manager、Encryption、Role 或 Redaction。Secret Manager/KMS/Vault/HSM 产品、算法、Rotation Period、Key Hierarchy、TLS/CA/证书部署配置 → 后续 Infrastructure/Deployment 决策（延迟产品/provider 选择与延迟实现决定）。**全局状态（Final Consistency Remediation）：** 本 Note 为渐进式历史记录（Status at Time of Decision）；其中「(PROPOSED/PENDING)」「仍 … PENDING」「留待 DQ-xx」「决定前」等表述反映对应周期时点状态。当前最终状态：全部 17 个 Decision Question 均已 ACCEPTED（Pending Decision Questions = 0）；RFC-002 = ACCEPTED（2026-08-04 用户正式决定；接受依据与授权边界见 RFC 主文档 §33 Decision Log 2026-08-04 Final Decision 记录；Acceptance ≠ Authorization，Implementation = NOT AUTHORIZED）。

---

## 0. 权威依据（官方研究结论摘要）

本矩阵的「DEC 已定边界」全部来自已 Accepted 的 DEC-012/013/024/025/029/032/033/035 与 RFC-001（DQ-04~08）。
本矩阵的「官方能力边界」来自四条一手研究（仅官方文档/官方源码，无博客）：

| 研究 | 关键官方边界 |
|---|---|
| **SQLAlchemy 2.x** | Session=UoW+identity map，生命周期须外置、事务要短、非并发（Session per thread）；`version_id_col` 原生乐观并发（仅 flush 单行生效）；`begin_nested`=SAVEPOINT 但 commit 总作用最外层；事务存续期独占 checkout 连接（池上限=pool_size+max_overflow，默认 5+10）；sync/async 两条独立 stack；commit 后 expire / close 后 detach；`with_for_update()` 渲染 FOR UPDATE 系列；ID 支持 client-side callable 与 server-side `server_default`/`Identity` |
| **LangGraph Checkpointer** | 生产推荐 PostgresSaver（SqliteSaver 不上生产、InMemory 仅测试）；4 张专用表 + setup()、无扩展依赖；checkpoint_id 单调（uuid6）可排序；interrupt 节点 resume **从头重执行**、interrupt 前副作用须幂等；**默认宽松反序列化有 RCE 风险，须 `LANGGRAPH_STRICT_MSGPACK=true`**；**Secret 会被明文序列化进 checkpoint**；Checkpointer=per-thread vs Store=cross-thread 两个不同原语；当前钉版 `prune` 不可用、无内建 TTL（官方建议应用层 cron 清理）；durability=sync/async/exit 默认 async、exit 仅退出时落库；**OSS 框架无同一 thread_id 并发 resume 的锁/乐观并发/CAS**（multitask 策略属 LangSmith Deployment） |
| **PostgreSQL / SQLite / Alembic** | PG：ACID + SAVEPOINT、MVCC、**无内建行 version 列**（乐观锁需应用层 version 列或引擎隔离级 40001 重试）、FOR UPDATE 悲观锁、jsonb+GIN、约束完备、TOAST 大文本/bytea/Large Object、快速加列 + `CREATE INDEX CONCURRENTLY` + `NOT VALID`+`VALIDATE` 两段式、DDL 事务性（除 CONCURRENTLY）。SQLite：ACID、**全库单写者**（WAL 下读者不阻塞写者）、多进程同机可读但任意时刻一写、网络文件锁不可靠、JSON=TEXT（无 GIN）。Alembic：autogenerate 必须人工 review（改名误判 add/drop）、forward-only 是项目策略非强制、batch mode（SQLite move-copy）、offline SQL、跨方言迁移脚本不可直接复用 |
| **模式定义** | Transactional Outbox（同库事务写 outbox + 独立 relay，at-least-once → 消费端必幂等）；relay=Polling（可移植）vs Log Tailing（低延迟、库特定、Debezium）；Idempotent Consumer/Receiver（dedup 表 + 主键判重、去重须与业务同事务）；Guaranteed Delivery（store-and-forward=Durable Dispatch 概念根源）；Fowler 四类事件（Notification/State Transfer/ES/CQRS 不可混淆）；Audit Log（append-only、问责、简单）≠ Domain Event（通知、触发）；完整 Event Sourcing 已 DEC-013 排除；Saga=长事务拆短本地事务+补偿 |

---

## 1. 边界矩阵（Topic → RFC-002 归属 → 后续 RFC 依赖 → 移交）

> **列含义：** `RFC-002 OWNS` = 本 RFC 必须给出 Decision；`INTERFACE` = 本 RFC 定义契约、后续 RFC 必须消费但不可改其语义；`DEFERRED` = 本 RFC 不决定、移交指定 RFC；`OUT OF SCOPE` = 本 RFC 明确不涉及。

| # | Topic | RFC-002 归属 | 依赖/移交 | 理由（官方/DEC 依据） |
|---|---|---|---|---|
| 1 | **主持久化技术（Business DB 引擎）** | **RFC-002 OWNS**（DQ-01 **ACCEPTED 2026-08-01**） | INTERFACE → RFC-003（Checkpointer 需同实例评估）、RFC-005（检索索引落点） | 业务库选型属持久化决策；SQLite 全库单写者 vs PG 行级 MVCC 是 DQ-01 权衡输入 |
| 2 | **ORM 与数据访问** | **RFC-002 OWNS**（DQ-01 **ACCEPTED 2026-08-01**） | INTERFACE → 全部 | SQLAlchemy sync-first 契合 RFC-001 DQ-07；detached/expire 行为是 Repository 不泄漏 ORM 实体的技术根因 |
| 3 | **持久化所有权 / 模块边界** | **RFC-002 OWNS**（DQ-02 **ACCEPTED 2026-08-01**） | INTERFACE → RFC-003/004/005（各模块表边界） | DEC-034 逻辑分离恒定；Shared Instance ≠ Shared Ownership |
| 4 | **Aggregate 与持久化边界** | **RFC-002 OWNS**（DQ-03 **ACCEPTED 2026-08-02**） | INTERFACE → 全部模块 | Atomic Business Commit 六要素单事务（DEC-035，提交协议） |
| 5 | **Domain State Versioning** | **RFC-002 OWNS**（DQ-04 **ACCEPTED 2026-08-02**） | INTERFACE → RFC-003（对账需读版本）、RFC-004（审核版本；revision → ETag/If-Match 映射可能，HTTP 协议未决） | DEC-024 六类版本指针 + DEC-029 并发版本未选型；三类分离 + `expected_revision` 协议已为用户决定；`xmin`/SERIALIZABLE 替代方案被拒绝 |
| 6 | **Transaction Boundary（Use Case↔事务）** | **RFC-002 OWNS**（DQ-05 **ACCEPTED 2026-08-02**） | INTERFACE → RFC-003（节点边界 / Resume 新事务）、RFC-007（超时/重试参数；锁与重试策略归 DQ-07） | 架构基线 §14.3 业务事务由 Use Case 拥有、§14.12 长流程多短事务；外部调用不入事务由连接 checkout 机制推断并已由用户正式决定为 PROHIBITED；默认 READ COMMITTED；SAVEPOINT 仅基础设施机制；嵌套/分布式事务拒绝；UoW Port 形态归 DQ-06、强隔离/锁/重试归 DQ-07 |
| 7 | **Unit of Work Model** | **RFC-002 OWNS**（DQ-06 **ACCEPTED 2026-08-02**） | INTERFACE → 全部 | SQLAlchemy Session=UoW（官方能力）；项目决定采用更严格一次性 UoW：Port 由 Application 定义、Infrastructure SQLAlchemy 实现（Session 为不暴露细节）、一 UoW / 一 Session / 一短事务 / 一最终结果、显式 commit（退出不自动提交）、未 commit/异常退出 rollback/close/discard、Repository 无事务控制权、嵌套业务 UoW 禁止、Composite 唯一外层 UoW、纯读独立短 Query Scope；并发/锁/重试归 DQ-07 |
| 8 | **Concurrency Control** | **RFC-002 OWNS**（DQ-07 **ACCEPTED 2026-08-02**） | INTERFACE → RFC-003（重复 resume / Checkpoint 并发对账）、RFC-004（并发编辑 / API 冲突协议）、RFC-007（运维重试指标与阈值） | DEC-022/029/033 五类并发场景；项目决定采用分层并发控制（乐观 revision 默认 + 命名唯一约束最终防线 + Durable Lease + 单调 fencing_token 执行所有权 + SKIP LOCKED 仅限队列式 Claim；Session-level Advisory Lock 禁止；40001/40P01 由 Application Transaction Runner 最多三次总尝试重试；语义冲突不盲目重试；Matrix 与多 Worker Technical Spike 为实现前置条件，均 REQUIRED 但未授权）；完整幂等键归 DQ-08 |
| 9 | **Idempotency Model** | **RFC-002 OWNS**（DQ-08 **ACCEPTED 2026-08-02**） | INTERFACE → RFC-003（resume 幂等：Runtime 负责 Attempt 与运行位置、Business Module 防止重复 Business Commit、Checkpoint 并发与 Runtime 对账）、RFC-004（submit 幂等：HTTP 幂等 Header / 状态码 / 响应协议） | DEC-033 幂等键/Input Fingerprint；Idempotent Consumer 权威；at-least-once→消费端幂等；项目决定采用分层幂等模型（各幂等层由 Owning Module 分层存储 + 统一语义契约，非统一物理表；Candidate A 跨模块万能表拒绝、Candidate B 为主 + Candidate C 强制设计原则；Retry/Rerun 身份语义；Versioned Input Fingerprint；同 Key 同 Fingerprint 重放、不同 Fingerprint 冲突；IN_PROGRESS 协同 DQ-07 Lease/fencing；DEC-035 同事务；Consumer Dedup 同事务；Provider Call Identity 复用；瞬时失败不固化、终局结果可重放；Idempotency Identity Matrix 为实现前置条件，REQUIRED 但未授权）；Outbox/Dispatch 归 DQ-09、Event/Audit 归 DQ-10、物理存储归 DQ-13、Retention 归 DQ-15、测试分类归 DQ-16、安全归 DQ-17 |
| 10 | **Transactional Outbox / Durable Dispatch** | **RFC-002 OWNS**（DQ-09 **ACCEPTED 2026-08-02**） | INTERFACE → RFC-003（Relay 与 Worker Backend 具体实现：direct PostgreSQL polling / LISTEN-NOTIFY acceleration / broker relay / worker lifecycle / deployment topology / runtime reconciliation / crash recovery；Ordering 实现与调度算法）、RFC-004（HTTP Status / Header / Response Body / Polling API 协议）、RFC-007（Work Execution Retry 策略：最大执行次数 / Backoff / Jitter / Dead-letter / Terminal Failure / 人工恢复 / 告警 / SLO） | RFC-001 DQ-07 已立 Durable Dispatch Boundary；项目决定采用 PostgreSQL-backed Transactional Durable Work Intent（Candidate B，Accepted with Major Revision；Candidate A 保留为未来 Integration Event Outbox 方向、Candidate C 仅作未来 Delivery Backend）：Durable Work Intent 与 Event/Audit/Checkpoint/Current Truth 语义分离；业务状态 + Intent 同一 Atomic Business Commit；API 仅在 Intent Commit 后返回 accepted；dispatch_id Retry 稳定 / Rerun 新建；短事务 SKIP LOCKED Claim + Lease Holder + fencing_token，长执行不持锁/Session/UoW；at-least-once Delivery（不承诺 exactly-once；唯一业务效果由 DQ-08 幂等 + Dedup + 唯一约束 + DQ-07 Lease/Fencing + revision + Atomic Commit 组合保证）；事务 Retry 与 Work Retry 分离；Polling 权威 / LISTEN-NOTIFY 非权威；现有 Matrix 须补充 Dispatch 场景（REQUIRED / NOT AUTHORIZED）；Event 分类归 DQ-10、Retention 归 DQ-15、Security 归 DQ-17、测试分类归 DQ-16 |
| 11 | **Event & Audit Persistence** | **RFC-002 OWNS**（DQ-10 **ACCEPTED 2026-08-02**） | INTERFACE → RFC-003（Event Relay / Broker / Polling / 发布状态机 / Dead-letter / 部署拓扑 / CloudEvents 版本与字段映射）、RFC-007（Observability Pipeline / Telemetry；Application Event Handler 失败观测告警恢复） | Audit Log≠Domain Event（Fowler，权威模式）；六类 Event/Record 独立语义（Candidate A，Accepted with Major Revision）；Universal Event / Audit Table（Candidate B）与全项目架构（Candidate C）拒绝；Audit Record append-only 同 DEC-035 事务（更正仅追加不覆盖）；State Transition Record 显式类型 Audit Record（物理共用 ≠ 语义合并，不充当 Integration Event / Current Truth）；Domain Event 默认不自动持久化；Application Event Commit 后 best-effort；Integration Event 可靠发布用独立 Transactional Integration Event Outbox（同 PostgreSQL 事务、at-least-once、Consumer Dedup、与 Work Intent 语义分离）；CloudEvents-compatible Envelope 可选互操作方向（不替代 Outbox/Dedup/Guarantee）；Observability Event 归 RFC-007 非权威 Telemetry（失败不回滚业务）；不构成 Business Current Truth、不引入 Event Sourcing（项目用户决定，非官方强制架构）；Classification Table REQUIRED / NOT AUTHORIZED；Retention 归 DQ-15、测试归 DQ-16、Security 归 DQ-17 |
| 12 | **Snapshot vs History** | **RFC-002 OWNS**（DQ-11 **ACCEPTED 2026-08-02**） | INTERFACE → RFC-003（恢复证据）、RFC-007（回放观测）；RFC-004（历史读取 API 路径/分页/Diff Response/Restore Endpoint/Authorization/用户可见性） | DEC-013 完整 ES 不属 MVP；Candidate A（Accepted with Major Revision）= Authoritative Current Truth + Immutable Business Version Snapshots + Append-only Audit/State Transition History + Optional Derived Query Projections；Full Event Sourcing（Candidate B，违反 DEC-013）/ Current-state-only Overwrite（Candidate C，违反 DEC-024）/ Delta-only 权威历史拒绝；正式版本逻辑完整、不可变、独立可读（不依赖 Event/Delta 重放、不经可变 Current Pointer 解释历史）；仅成功 Atomic Business Commit 产生正式版本；Current Truth 由带独立 revision 的显式 Current Truth Pointer 经 CAS 选择（`MAX(version_number)` 禁止）；Invalidation 不删除且不静默回退；Restore 为新前向 Business Command（非数据库 Rollback）；Query Projection 派生非权威；不引入通用 Bitemporal Model / SQL AS-OF；五种 Snapshot 术语独立（项目用户决定，非官方强制架构）；Aggregate / Invariant Matrix 扩展 REQUIRED / NOT AUTHORIZED；Source/Evidence 边界归 DQ-12、Schema 演进归 DQ-14、Retention 归 DQ-15、测试归 DQ-16、Security 归 DQ-17 |
| 13 | **Source & Evidence Persistence** | **RFC-002 OWNS**（DQ-12 **ACCEPTED 2026-08-02**） | INTERFACE → RFC-005（Retrieval Index / Chunking / Embedding / Vector Store / Ranking / Search Runtime / Reindex-Rebuild）；RFC-004（Presigned URL 等对象访问协议）；RFC-007（Missing/Corrupt Object 恢复与运营流程） | DEC-025 Source/Evidence 独立语义；**DQ-12 Accepted Decision（Candidate A，Accepted with Major Revision）：PostgreSQL Authoritative Source/Evidence Graph + Immutable Content-addressed Source Blobs + Versioned Derived Artifacts and Fragments + Explicit Evidence-to-Claim Links + Rebuildable Non-authoritative Retrieval Index；Universal All-in-PostgreSQL（Candidate B）/ Universal All-in-Object-Storage（Candidate C）拒绝；PostgreSQL 为权威身份/关系/状态/Provenance；TOAST 非业务边界、Large Object 非默认路径；Raw 不可变、Derived 版本化；ETag 非权威 Content Hash；不假设分布式事务、Upload→Verify→PostgreSQL Finalize；EvidenceLink 固定不可变 SourceVersion、不只引用 Vector ID；Retrieval Index 派生/可重建/非权威；Source & Evidence Storage Classification Table 与 External Object Consistency Technical Spike REQUIRED / NOT AUTHORIZED；Retention→DQ-15、Security→DQ-17** |
| 14 | **Workflow Checkpoint Separation** | **RFC-002 OWNS**（DQ-13 **ACCEPTED 2026-08-02**） | INTERFACE → RFC-003（生产 Checkpointer 选型 / durability 逐节点策略 / serde / 并发防护 / Runtime Lifecycle / Runtime 对账）/ RFC-007（Missing/Corrupt Checkpoint 与运营恢复） | DEC-023/024 Checkpoint 仅恢复、≠Current Truth；DEC-033 Reconciliation；**DQ-13 Accepted Decision（Candidate A，Accepted with Major Revision）：Shared PostgreSQL Service + Isolated Checkpoint Persistence Plane + Dedicated Role/Connection Pool/Storage Namespace + Application-owned Workflow Execution Registry + Business-Current-Truth-first Reconciliation；Independent PostgreSQL Service（Candidate B）/ 同表混存（Candidate C）拒绝；优先同 Database + Dedicated Checkpoint Schema（必须用钉定 PostgresSaver + 实际 Pool/Pooler 证明，不假设），Fallback = 同 Service 内 Dedicated Checkpoint Database；Business/Registry/Vendor Checkpoint 三平面分离、独立 Pool/Role；Checkpoint = Runtime Recovery State only、≠Business Current Truth；Business Commit 与 Checkpoint Write 不构成同一 Atomic Transaction；Resume 必须 Business-Current-Truth-first Reconciliation（RESUMABLE/ALREADY_COMMITTED/STALE/SUPERSEDED/INVALIDATED/ORPHANED/INCOMPATIBLE/CORRUPT）；Checkpoint 不承担并发控制（DQ-07 Lease/Fencing）；Graph Time Travel/Fork ≠ Business Restore；Vendor Setup/Migration 与 Business Alembic 分离；默认 Whole-thread Deletion、未验证禁止 Partial Pruning；Workflow Checkpoint Boundary & Reconciliation Table 与 Workflow Checkpoint Isolation & Reconciliation Technical Spike REQUIRED / NOT AUTHORIZED；Retention→DQ-15、Serializer 安全→DQ-17、测试→DQ-16** |
| 15 | **Schema Evolution & Migrations** | **RFC-002 OWNS**（DQ-14 **ACCEPTED 2026-08-03**） | INTERFACE → 全部 | **DQ-14 Accepted Decision（Candidate A，Accepted with Major Revision）：Alembic-managed Business Schema Migrations + Single Business Migration Lineage + Forward-recovery-first Production Policy + Expand-Migrate-Contract Rolling Compatibility + Resumable Application-owned Data Backfills + Explicit Destructive/Non-transactional DDL Gates + Separate Vendor Migration Lifecycles；Universal Safe Downgrade（Candidate B）/ Sole Hand-written SQL（Candidate C）拒绝；单一 Business Head + 已发布 Revision 不可变 + Forward Repair；Autogenerate 仅 Candidate Generator 必经人工审查；alembic check 为 drift 检测非安全证明；Expand 与 Contract 不同一次发布；大型 Backfill 不入长 Alembic Transaction（Resumable/分批/幂等/Lease+Fencing）；Technical Backfill ≠ Business Semantic Change；CREATE INDEX CONCURRENTLY 独立 Non-transactional Boundary；Business Alembic ≠ Vendor Migration ≠ Retrieval Index Rebuild ≠ Object Storage Lifecycle；alembic_revision ≠ domain_version_id/version_number/revision/checkpoint_schema_version 等，Alembic Head ≠ Backfill/Validation/Contract 完成；Schema Migration Compatibility & Risk Table 与 Schema Migration Rollout & Recovery Technical Spike REQUIRED / NOT AUTHORIZED；不创建真实迁移；完整测试分类→DQ-16、Retention→DQ-15、Security→DQ-17** |
| 16 | **Data Retention & Deletion Boundary** | **RFC-002 OWNS**（DQ-15） | INTERFACE → RFC-003（Checkpoint 清理执行协调 / Backup-PITR 基础设施 / Purge 运行时治理）、RFC-005（Retrieval Index/Cache/Vector/Embedding Metadata 删除范围）、RFC-007（日志保留不替代 Audit / Incident 运营） | DEC-013/025 保留周期未确认；checkpoint 官方无内建 TTL、应用层 cron；**DQ-15 ACCEPTED 2026-08-03（Accepted with Major Revision：Candidate A——Classified Retention & Disposition Policies + Purpose/Legal-basis-driven Retention Clocks + Reference-aware Deletion Eligibility + Legal/Security/Incident Hold Overrides + Normal-lifecycle Immutability for Business History + Governed Exceptional Erasure/Redaction Paths + Idempotent Auditable Purge Orchestration + Separate Primary/Object/Index/Checkpoint/Backup Lifecycles；Universal TTL（Candidate B）/ Universal Permanent Retention（Candidate C）拒绝；不设置或虚构具体保留周期（`PERIOD NOT DECIDED`）；Invalidation/Archive/Soft Delete/Encryption ≠ Physical Purge；Retention Clock 数据类专属（不统一 created_at）；业务历史正常生命周期不物理删除（受治理 Exceptional Erasure/Redaction Path 必需，精确机制留 DQ-17（已由 DQ-17 于 2026-08-03 正式决定））；删除 Reference-aware（广泛跨模块 ON DELETE CASCADE 禁止）；Hold 阻止 Physical Purge；Checkpoint 继续 DQ-13 Whole-thread Deletion；Work Intent/Outbox/Dedup/Idempotency 正确性窗口内不删；Raw Provider/Model Payload 不默认永久保存；中央治理 + 分散所有权（Orchestrator 不跨模块直接 SQL 删除，经类型化 Application Contract）；删除 = 受治理 Deletion/Erasure Case（非单次 SQL DELETE）；Purge 幂等/可审计/Lease+Fencing/分批短事务；Primary Purge ≠ Physical Reclamation ≠ Backup Expiry；Backup Restore 重放 Deletion Ledger；Tombstone/Deletion Proof 不含原敏感载荷；Data Retention, Hold & Deletion Policy Table 与 Retention & Deletion Safety Technical Spike REQUIRED / NOT AUTHORIZED；具体保留周期留未来业务/合规/合同/安全/运营决定；测试分类→DQ-16、Security/PII/Redaction/Anonymization→DQ-17** |
| 17 | **Persistence Testing Strategy** | **RFC-002 OWNS**（DQ-16） | INTERFACE → 全部（测试基建原则）；第一个获授权 Persistence Spike（Common Harness Qualification 落地） | **DQ-16 ACCEPTED 2026-08-03（Accepted with Major Revision：Candidate A——Layered Test Strategy：Pure Domain/Application Unit Tests + Port Contract Parity Tests + Real PostgreSQL Persistence Acceptance Tests + Deterministic Multi-connection Concurrency Tests + Real Migration/Upgrade/Recovery Tests + Crash-window/Fault-injection Tests + Production-topology-specific Qualification；全部真实 PostgreSQL 通用策略（Candidate B）/ 全部 SQLite fake（Candidate C）拒绝；SQLite = 可选非权威 Test Double（Persistence Acceptance Engine 禁止）；正式持久化语义必须真实 PostgreSQL（Fake Pass ≠ PostgreSQL Adapter Pass）；MVP 只测 SQLAlchemy 2.x sync + Psycopg 3 sync（Async 不属 MVP 验收范围）；钉定版本 + 独立 Test Role + 隔离 Database/Schema + 正式 Alembic Migration；多连接/Commit Visibility/Crash/Migration 必须真实 Commit；并发 Actor 独立 Connection/Session/Transaction + 确定性协调（不主要依赖 sleep）；DEC-035 Fault Injection 全有或全无；9 类 Crash Window；Migration Test 真实 PostgreSQL（metadata.create_all() 不作 Migration Acceptance）；Required PR Checks 含 correctness-critical PostgreSQL 测试；禁止自动 Retry-to-green；合成数据无真实 Secret（精确 Fixture 规则留 DQ-17，已由 DQ-17 于 2026-08-03 正式决定）；Persistence Test Coverage & Fidelity Table REQUIRED / NOT AUTHORIZED；不新增独立通用 Spike；Common Persistence Test Harness Qualification = REQUIRED AS PART OF FIRST AUTHORIZED PERSISTENCE SPIKE / NOT AUTHORIZED；不授权创建测试/Harness/Container/CI Job/Fixture/Contract Suite/测试代码、不修改 CI Workflow）** |
| 18 | **Security & Sensitive Data Boundary** | **RFC-002 OWNS**（DQ-17） | INTERFACE → RFC-003（Graph State/Checkpoint Runtime Configuration）、RFC-004（API Authentication/Authorization/Transport）、RFC-006（LLM and Provider Secret Injection）、RFC-007（Logging/Trace Redaction/Security Monitoring）；Secret Manager/KMS/Vault/HSM 产品与算法/TLS-CA-证书部署配置 → 后续 Infrastructure/Deployment 决策 | **DQ-17 ACCEPTED 2026-08-03（Accepted with Major Revision：Candidate A——Formal Model = Classified Sensitive-data Protection + Secret-reference-only Persistence + Ephemeral Adapter-scoped Secret Resolution + Data-minimized Multi-plane Propagation + Strict Allowlisted Checkpoint Serialization + Least-privilege Role/Credential/Pool Separation + Selective Envelope Encryption by Protection Profile + Infrastructure Encryption in Transit/at Rest + Auditable Access/Redaction/Rotation/Incident Response；Candidate B 不作 Universal Field Encryption（Selective Envelope Encryption 按 Data Protection Profile 条件启用）、Candidate C 不作 Sole Security Control（Infrastructure At-rest Encryption 保留为 REQUIRED INFRASTRUCTURE DEFENSE IN DEPTH）；Secret Value 禁止进入任何持久化或派生存储（仅无明文能力引用；Adapter ephemeral 解析）；数据分类 Confidentiality Level + Handling Tags 传播全部平面；SecretStr/Masked repr ≠ Non-persistence Guarantee；LANGGRAPH_STRICT_MSGPACK REQUIRED + Pickle Fallback PROHIBITED；ENCRYPTED CHECKPOINT ≠ PERMISSION TO STORE SECRETS；三层加密责任（Transport/At-rest Baseline/Selective Envelope）；Key-Ciphertext 分离；禁止未经治理加密；PostgreSQL Least Privilege 8 类 Role/Pool（Runtime Role 非 Superuser/Table Owner/BYPASSRLS）；RLS = OPTIONAL DEFENSE IN DEPTH；Redaction = CLASSIFY→MINIMIZE→REDACT→SERIALIZE/EMIT/PERSIST；Provider/Prompt/Model Output = 不可信敏感输入；CROSS-TENANT/CROSS-SECURITY-DOMAIN DEDUP = PROHIBITED FOR MVP（DQ-12 暂缓边界正式关闭）；测试仅合成数据、Secret Detection Failure = Merge Blocker；Sensitive Data, Secret & Cryptographic Control Matrix REQUIRED / NOT AUTHORIZED；不新增独立通用 Spike；Cross-cutting Persistence Security Qualification = REQUIRED IN FIRST AUTHORIZED PERSISTENCE SPIKES / NOT AUTHORIZED；Selective Field Encryption & Key Rotation Spike = CONDITIONALLY REQUIRED / NOT AUTHORIZED；不选择 KMS/Vault/HSM/算法/Rotation Period；不创建 Secret Manager/Encryption/Role/Grant/RLS/Redaction/Security Audit/Secret Scanner/测试）** |

---

## 2. RFC-002 必须**不**决定的后续 RFC 主题（显式 DEFERRED / OUT OF SCOPE）

下列主题**不是** RFC-002 的决策范围。RFC-002 仅为它们提供**持久化契约/落库形态**，绝不替它们做实现或选型决定：

| 主题 | 归属 RFC | RFC-002 的正确姿态 |
|---|---|---|
| LangGraph 生产 Checkpointer **具体实现**（PostgresSaver 配置、durability 模式、serde 加密、interrupt 节点结构） | **RFC-003** | RFC-002 只定「Checkpoint 与业务库的物理/逻辑边界 + 对账权威」；durability/serde/节点结构移交 |
| LangGraph 节点结构 / Graph 拓扑 / Node 拆分 | **RFC-003** | 完全不涉及；仅引用 DEC-035 已定的分节点契约 |
| 生产 dispatch backend（Relay / Worker Backend / Broker / 部署拓扑具体实现） | **RFC-003** | RFC-002 已决定（**DQ-09 ACCEPTED 2026-08-02**）「PostgreSQL-backed Transactional Durable Work Intent 语义」（同事务创建、accepted 边界、Dispatch Identity、Envelope、生命周期、Claim/Lease/Fencing、Completion Atomicity、at-least-once、两层 Retry 分离、Polling 权威恢复路径）；Relay / Worker Backend 具体实现（direct PostgreSQL polling / LISTEN-NOTIFY acceleration / broker relay / worker lifecycle / deployment topology / runtime reconciliation / crash recovery）与 Ordering 调度算法移交；独立 Message Broker 不在 MVP 引入（未来仅作 Delivery Backend，不替代事务内 Durable Intent） |
| API endpoints / HTTP schema / Human Review 提交协议 / 权限 | **RFC-004** | RFC-002 只定「Review 相关持久化实体 + 并发控制 + 幂等键」；HTTP 层移交 |
| Retrieval chunking / 向量库 / embedding 模型 / 检索索引实现 / 证据装配 | **RFC-005** | RFC-002 只定「Source/Fragment/Evidence 的持久化形态 + 索引落点边界」；检索实现移交 |
| LLM Provider / Prompt runtime / 结构化输出 / 模型 Secret 注入 | **RFC-006** | RFC-002 只定「LLM 执行记录的持久化形态 + Secret 不落 Graph State/checkpoint」；LLM 实现移交 |
| Logging vendor / Metrics backend / Tracing / OpenTelemetry / Retry-Timeout-Backoff-CircuitBreaker 生产参数 | **RFC-007** | RFC-002 只定「审计/事件/运行记录的持久化形态」（**DQ-10 ACCEPTED 2026-08-02**：Observability Event 属 RFC-007 非权威 Telemetry——不是 Business Current Truth / Audit Record / State Transition Record / Idempotency Record / Durable Work Intent / Integration Event；Telemetry 失败不得回滚业务事务、不得替代 Audit Record；Application Event Handler 失败的观测/告警/恢复归 RFC-007 或相应 Runtime RFC）；观测栈移交 |
| Deployment topology / 容器 / 托管平台 | **超出全部 RFC（后续）** | 不涉及 |

---

## 3. 关键「边界即事实」校正（避免越权或误述）

1. **「Business Database = Current Truth / Checkpointer = Recovery」非字面等号短语。** 权威原文为 architecture-baseline-v1 §2「三类存储分离：Business（Current Truth）/ Runtime（执行记录）/ Checkpoint（Graph 检查点）物理分离；**Checkpoint ≠ Current Truth**」与 DEC-023「LangGraph Checkpointer **仅**承载执行恢复、图状态快照、Interrupt 和 Resume」。RFC-002 一律引用真实原文。
2. **Checkpoint 分离是项目契约（DEC-024/033），非 LangGraph 官方强制。** 官方对「Checkpointer 与业务库同库/分库」**无建议**——这是 RFC-002/DQ-13 的真实决策空间，不得假装是官方约束。
3. **「不得跨外部调用持有 DB 事务」原为架构推断（Recommendation），官方事实仅「事务存续期独占 checkout 连接、池有上限、超时报错」，官方未以「建议」形式写明。此条已由 **DQ-05 Accepted Decision（2026-08-02）** 正式决定为 PROHIBITED（项目用户决定；官方仍未明令）。
4. **Traceability matrix 用 legacy 字母码：** RFC-B≈RFC-002、RFC-C≈RFC-003、RFC-D≈RFC-004、RFC-E≈RFC-005、RFC-F≈RFC-006、RFC-G≈RFC-007、RFC-A≈RFC-001。RFC-002 正文引用时须用正式编号并注明映射。
5. **durability / pending-writes / 同一 thread_id 并发防护属 RFC-003**（它们依赖生产 Checkpointer 选型与 Graph 结构）；RFC-002 只提供「Checkpoint 不存业务真值、可视为可回收执行副产物」的落库边界，**不**替 RFC-003 选 durability 模式或并发防护机制。
6. **Outbox relay（Polling vs Log Tailing/Debezium）与 dispatch backend 属 RFC-003**；RFC-002/DQ-09 已决定（**2026-08-02 ACCEPTED**）：MVP 采用 PostgreSQL-backed Transactional Durable Work Intent（Durable Intent 语义、Envelope、生命周期、Claim/Lease/Fencing、Completion Atomicity、at-least-once Delivery、两层 Retry 分离、Polling 权威恢复路径；LISTEN/NOTIFY 仅非权威唤醒优化，不作为可靠队列或 Delivery Guarantee），Relay 与 Worker Backend 的具体实现（direct PostgreSQL polling / LISTEN-NOTIFY acceleration / broker relay / worker lifecycle / deployment topology / runtime reconciliation / crash recovery）与 Ordering 调度算法移交 RFC-003。
7. **Event / Audit 分类、Audit append-only 同事务、Transactional Integration Event Outbox 与 Event & Record Classification Table 实现前置均为项目用户决定（DQ-10 ACCEPTED 2026-08-02），非 Fowler / CNCF CloudEvents / PostgreSQL 官方强制架构。** Fowler Audit Log ≠ Domain Event 分界与四类事件、Transactional Outbox / Idempotent Consumer 模式是官方/权威模式能力；CloudEvents 是 CNCF 官方事件格式标准，**但 CloudEvents-compatible Envelope 不替代 Transactional Outbox / Atomic Commit / Relay / Retry / Consumer Dedup / Delivery Guarantee**（只解决事件格式和互操作元数据）；Event Relay / Broker / 发布状态机 / Dead-letter / 部署拓扑与 CloudEvents 版本 / 字段映射 / 协议选择移交 RFC-003 或独立 Integration Design；Observability Event 属 RFC-007 非权威 Telemetry（失败不回滚业务、不替代 Audit）；Retention / Security / 测试分类移交 DQ-15/16/17；**Classification Table 创建 REQUIRED 但 NOT AUTHORIZED，不得借 DQ-10 归档创建 Audit Schema / Event Schema / Event Registry / Audit 表 / State Transition Ledger / Event Outbox / Publisher / Relay / Consumer / Migration**。
8. **Snapshot / History 模型、逻辑完整 Business Snapshot、显式 Current Truth Pointer、Restore 前向命令、Query Projection 非权威、不引入通用 Bitemporal Model 与 Aggregate / Invariant Matrix 扩展列均为项目用户决定（DQ-11 ACCEPTED 2026-08-02），非 Fowler / PostgreSQL / 任何第三方官方强制架构。** Fowler Event Sourcing 分界（「keeping a history or writing to a log file could give you an adequate history」；ES 额外之处仅在「用事件重建当前状态」）、Martin Fowler 版本化/不可变快照与 bitemporal 概念、PostgreSQL MVCC（行级多版本并发控制）、PostgreSQL Backup / PITR（基础设施灾难恢复副本）、PostgreSQL Temporal 扩展（`valid_from`/`valid_to` 系统版本时间）= **官方能力 / 权威模式**；**但 Authoritative Current Truth + Immutable Business Version Snapshots + Append-only Audit/Transition History + Optional Derived Query Projections 的组合模型、逻辑完整快照语义（不依赖 Event/Delta 链重放、不经可变 Current Pointer 解释历史）、显式 Current Truth Pointer（带独立 revision、CAS 选择、`MAX(version_number)` 禁止、latest created/approved/current effective 独立语义）、Invalidation 不删除且不静默回退、Restore 作为新前向 Business Command（非数据库 Rollback）、Query Projection 派生非权威、仅成功 Atomic Business Commit 产生正式版本、不引入通用 Bitemporal Model / 全项目 SQL `AS OF` 查询层、五种 Snapshot 术语独立、Aggregate / Invariant Matrix 扩展列（Version Boundary / Snapshot Granularity / Current Truth Selector / Promotion / Invalidation / Restore / Historical Dependency）与 DQ-07 Spike 扩展（版本分配/Pointer CAS/Invalidation/Restore 竞争）均为 Accepted Architecture Decision（项目用户决定），非 Fowler / PostgreSQL / 任何第三方官方强制架构**；**不得把 PostgreSQL Backup / PITR、Temporal Feature 或数据库 MVCC 描述成 Business Version History，不得把 Event-Sourcing Snapshot 描述成当前项目正式 Business Snapshot**；历史读取 API 契约（路径/分页/Diff Response/Restore Endpoint/Authorization）移交 RFC-004；Source/Evidence/大对象边界归 DQ-12、Snapshot Schema Evolution/Upcasting/数据迁移归 DQ-14、Retention 归 DQ-15、Security 归 DQ-17、测试分类归 DQ-16；**Aggregate / Invariant Matrix 扩展 REQUIRED 但 NOT AUTHORIZED，不得借 DQ-11 归档创建或修改 Matrix、Version Tables、Current Truth Pointer、Projection、Restore / Promotion / Invalidation 实现、Snapshot Schema 或 Migration**。
9. **Source/Evidence Graph、Content-addressed Object Key、Upload/Verify/Finalize 协议、SourceVersion/Fragment/EvidenceLink 模型、Storage Classification Table 与 External Object Consistency Spike 均为项目用户决定（DQ-12 ACCEPTED 2026-08-02），非 PostgreSQL / 对象存储 Provider / W3C 官方强制架构。** PostgreSQL TOAST / bytea / Large Object / jsonb+GIN、对象存储 ETag / Conditional Put / Multipart Checksum / Read-after-write、W3C PROV Entity-Activity-Agent 模型 = **官方能力 / 公开标准**；**但 PostgreSQL Authoritative Source/Evidence Graph、Source/SourceVersion/ContentObject/Acquisition/DerivedArtifact/Fragment/EvidenceLink 身份分离模型、显式 Storage Classification Policy、Content-addressed Object Key（`<algorithm>/<content-hash>`）、Prepare→Upload→Verify→Finalize 一致性协议、Orphan/Integrity Incident 语义、EvidenceLink 固定不可变 SourceVersion、Retrieval Index 派生/可重建/非权威、Source & Evidence Storage Classification Table 与 External Object Consistency Technical Spike 均为 Accepted Architecture Decision（项目用户决定），非 PostgreSQL、对象存储 Provider 或 W3C 官方强制架构**；**不得把 TOAST 描述成业务 Storage Policy 或大小边界、不得把 PostgreSQL Large Object 描述成默认方案、不得把对象 ETag 描述成项目权威 Content Hash、不得把 Retrieval Index 描述成 Business Current Truth、不得把 Search Result 描述成正式 Evidence**；Retrieval Index / Chunking / Embedding / Vector Store 归 RFC-005、Presigned URL 等对象访问协议归 RFC-004、Missing/Corrupt Object 恢复运营归 RFC-007、Retention/Orphan Grace Period 归 DQ-15、Encryption/Redaction/PII/Object Key Security/跨安全域去重归 DQ-17、测试分类归 DQ-16；**Source & Evidence Storage Classification Table 与 External Object Consistency Technical Spike REQUIRED 但 NOT AUTHORIZED，不得借 DQ-12 归档创建 Classification Table、Source/SourceVersion/ContentObject/Acquisition/DerivedArtifact/Fragment/EvidenceLink 表、Object Storage Bucket/Adapter、Parser/Normalizer、Retrieval Index、Migration 或选择对象存储 Provider**。
10. **Shared PostgreSQL Service + Isolated Checkpoint Plane、Dedicated Schema（必须验证）/ Dedicated Database Fallback、Workflow Execution Registry、Reconciliation 状态模型、Whole-thread Deletion、Workflow Checkpoint Boundary & Reconciliation Table 与 Isolation Spike 均为项目用户决定（DQ-13 ACCEPTED 2026-08-02），非 LangGraph / PostgreSQL / Psycopg 官方强制架构。** LangGraph Checkpointer（PostgresSaver 4 张专用表 + setup()、checkpoint_id 单调可排序、`delete_thread` 可用、当前钉版 `prune` 不可用、无内建 TTL、durability=sync/async/exit、OSS 无同一 thread_id 并发防护）、PostgreSQL Schema/Role/Connection Pool/search_path、Psycopg Pool 与 Pooler（Transaction/Session Mode）= **官方能力**；**但 Shared PostgreSQL Service + Isolated Checkpoint Persistence Plane、三个持久化平面分离（Business Persistence / Application-owned Workflow Execution Registry / Vendor Checkpoint）、Dedicated Schema 必须用钉定 Python PostgresSaver + 实际 Pool/Pooler 证明（不假设原生支持）、Dedicated Database Fallback、Workflow Execution Registry 显式映射、Business-Current-Truth-first Reconciliation 八状态分类（RESUMABLE/ALREADY_COMMITTED/STALE/SUPERSEDED/INVALIDATED/ORPHANED/INCOMPATIBLE/CORRUPT）、Checkpoint ≠ Business Current Truth、Business Commit 与 Checkpoint Write 非同一事务、Checkpoint 不承担并发控制（DQ-07 Lease/Fencing）、Graph Time Travel/Fork ≠ Business Restore、Vendor Setup 经受控部署/Migration Job、Vendor Migration 与 Business Alembic 分离、Whole-thread Deletion 默认、Workflow Checkpoint Boundary & Reconciliation Table、Workflow Checkpoint Isolation & Reconciliation Technical Spike 均为 Accepted Architecture Decision（项目用户决定），非 LangGraph、PostgreSQL 或 Psycopg 官方强制架构**；**不得声称 Python PostgresSaver 已被证明原生支持项目要求的 Dedicated Schema、不得声称 Checkpoint 与 Business Commit 是同一事务、不得声称 Checkpoint 提供并发锁、不得声称 thread_id 是业务身份、不得声称 Checkpoint Time Travel 是 Business Restore、不得声称 Partial Pruning 已被安全验证**；生产 Checkpointer 选型/durability/serde/并发防护归 RFC-003、Retention 归 DQ-15、Serializer 安全归 DQ-17、测试归 DQ-16；**Workflow Checkpoint Boundary & Reconciliation Table 与 Workflow Checkpoint Isolation & Reconciliation Technical Spike REQUIRED 但 NOT AUTHORIZED，不得借 DQ-13 归档创建 Workflow Execution Registry、Checkpoint Schema/Database、Role、Pool、PostgresSaver Setup、Vendor Tables、Migration、Serializer、Reconciler、Retention Job、测试或任何 Runtime/Persistence 实现**。
11. **Forward-recovery-first 生产恢复策略、Single Business Migration Head 单一谱系、Migration History Immutability、Expand-Migrate-Contract 滚动兼容、Resumable Application-owned Backfill、Reversibility Classification、Destructive/Non-transactional DDL Gate、Technical Backfill ≠ Business Semantic Change、Business Alembic ≠ Vendor Migration 分离、Schema Migration Compatibility & Risk Table 与 Rollout Spike 均为项目用户决定（DQ-14 ACCEPTED 2026-08-03），非 Alembic / PostgreSQL 官方强制架构。** Alembic autogenerate（必须人工 review，改名误判 add/drop）、forward-only（项目策略非强制）、batch mode、offline SQL、PostgreSQL 快速加列 / `CREATE INDEX CONCURRENTLY` / `NOT VALID`+`VALIDATE` 两段式 / DDL 事务性（除 CONCURRENTLY）= **官方能力**；**但 Forward-recovery-first（生产恢复不依赖通用 Schema Downgrade）、Single Business Migration Head（Merge/Release Gate 单一 Head）、已发布 Revision 不可变（出错创建 Forward Repair）、Expand-Migrate-Contract（Expand 与 Contract 不同一次发布）、Resumable Application-owned Backfill（不入长 Alembic Transaction，分批/幂等/Lease+Fencing）、Reversibility Classification（REVERSIBLE_SCHEMA/FORWARD_FIX_ONLY/DATA_IRREVERSIBLE/NON_TRANSACTIONAL_DDL/DESTRUCTIVE_CONTRACT/VENDOR_MANAGED）、Destructive Migration Gate / Non-transactional DDL Gate、Technical Backfill ≠ Business Semantic Change（后者经正式 Business Application Contract + Audit + 版本化规则）、Business Alembic Migration ≠ PostgresSaver Vendor Migration ≠ Retrieval Index Rebuild ≠ Object Storage Lifecycle、Schema Version Identity Separation（alembic_revision ≠ domain_version_id/version_number/revision/checkpoint_schema_version/graph_definition_version/event_schema_version/payload_schema_version/source_artifact_schema_version；Alembic Head ≠ Backfill/Validation/Contract 完成）、Schema Migration Compatibility & Risk Table、Schema Migration Rollout & Recovery Technical Spike 均为 Accepted Architecture Decision（项目用户决定），非 Alembic、PostgreSQL 官方强制架构**；**不得声称 Alembic Autogenerate 结果天然安全、所有 Revision 都可无损 Downgrade、`alembic check` 等于 Migration 安全证明、到达 Alembic Head 表示 Backfill/Validation/Contract 完成、Business Alembic 管理 Vendor Checkpoint Tables、所有 PostgreSQL DDL 都是完整事务性、`CREATE INDEX CONCURRENTLY` 可放入普通 Transaction Block、DQ-14 已授权创建 Migration**；完整测试分类归 DQ-16、Retention 物理删除归 DQ-15、Security/PII 归 DQ-17、生产 Checkpointer/Vendor Migration 独立规则归 RFC-003、Retrieval Index Rebuild 归 RFC-005；**Schema Migration Compatibility & Risk Table 与 Schema Migration Rollout & Recovery Technical Spike REQUIRED 但 NOT AUTHORIZED，不得借 DQ-14 归档创建 Alembic Environment/Config/Revision/Migration Script/Migration Job/Migration Executor/Backfill Registry/Backfill Worker/DDL/Constraint/Index/Trigger/Readiness Table/Technical Spike/测试/部署配置或修改 Production Database/Vendor Checkpoint Tables**。
12. **分类保留与处置策略、目的/法律依据驱动的保留时钟、引用感知删除资格、Hold 覆盖、正常生命周期不可变与受治理例外擦除路径、幂等可审计清除编排、分存储平面独立生命周期、中央治理 + 分散数据所有权、受治理 Deletion/Erasure Case、Deletion State Machine、最小 Tombstone/Deletion Proof、Data Retention, Hold & Deletion Policy Table 与 Retention & Deletion Safety Technical Spike 均为项目用户决定（DQ-15 ACCEPTED 2026-08-03），非 PostgreSQL / LangGraph / 任何法规官方强制架构。** PostgreSQL `DELETE`/VACUUM/物理空间回收、事务性 DDL、Backup/PITR 与 Restore 机制、对象存储 Lifecycle/Versioning/Object Lock 能力、LangGraph Checkpointer `delete_thread`/官方建议应用层 cron 清理（TTL 仅属 Store 非 Checkpointer）= **官方能力**；**但 Classified Retention & Disposition Policies + Purpose/Legal-basis-driven Retention Clocks + Reference-aware Deletion Eligibility + Legal/Security/Incident Hold Overrides + Normal-lifecycle Immutability for Business History + Governed Exceptional Erasure/Redaction Paths + Idempotent Auditable Purge Orchestration + Separate Primary/Object/Index/Checkpoint/Backup Lifecycles 的 Formal Model、12 类生命周期术语独立（Invalidation/Supersession/Archive/Access Restriction/Redaction/Pseudonymization/Anonymization/Tombstone/Logical Deletion/Physical Purge/Backup Expiry 互不等同）、数据类专属 Retention Clock（不统一 created_at）、引用感知资格（广泛跨模块 ON DELETE CASCADE 禁止、跨 Tenant/主体/安全域误删阻止）、Hold 阻止 Physical Purge 且不被普通 Job 解除、Checkpoint 继续 DQ-13 Whole-thread Deletion、Work Intent/Outbox/Dedup/Idempotency 正确性窗口内不删、Raw Provider/Model Payload 不默认永久保存、Central Retention Governance + Decentralized Data Ownership（中央 Purge Orchestrator 不跨模块直接 SQL 删除，经目标模块类型化 Application Contract）、受治理 Deletion/Erasure Case（非单次 SQL DELETE）、Deletion State Machine 13 态（BACKUP_EXPIRY_PENDING 合法中间状态、COMPLETED 不在单表删除后设置）、Purge 幂等/可审计/Lease+Fencing/分批短事务/Crash Recovery（重复执行不扩大范围）、Application Inaccessibility/Logical Deletion/Physical Reclamation/Backup Expiry 分离（SQL DELETE Commit 不表字节立即消失）、Backup Restore 重放 Deletion Ledger、最小 Tombstone/Deletion Proof 不含原敏感载荷、Data Retention, Hold & Deletion Policy Table、Retention & Deletion Safety Technical Spike 均为 Accepted Architecture Decision（项目用户决定），非 PostgreSQL、LangGraph 或任何法规官方强制架构**；**不得声称某一法律要求所有数据立即删除、所有 Business History 在任何法律情况下都绝对不可处置、Soft Delete 等于 Physical Purge、Archive 等于 Delete、SQL DELETE Commit 表示底层字节立即消失、Primary Purge 表示 Backup 已完成处置、Backup Restore 可以不重放 Deletion Ledger 即重新暴露已删除数据、Deletion Proof 可以保存完整原敏感载荷、中央 Purge Orchestrator 可以跨模块直接 SQL 删除、Hold 可以被普通 Retention Job 自动解除、DQ-15 已决定具体保留期限、DQ-15 已授权执行数据删除**；具体保留期限 → 未来业务/合规/合同/安全/运营决定 + Policy Table（延迟法律/合规决定）、精确 PII 分离/Redaction/Anonymization/Encryption/法律例外机制 → DQ-17、Retention/Purge 测试分类 → DQ-16、Backup/PITR 周期与基础设施 → 后续独立决定（延迟实现决定）；**Data Retention, Hold & Deletion Policy Table 与 Retention & Deletion Safety Technical Spike REQUIRED 但 NOT AUTHORIZED，不得借 DQ-15 归档创建 Retention Policy Registry/Policy Table/Deletion-Erasure Case/Hold Registry/Purge Worker/Deletion Ledger/Tombstone/Retention Job/Object Lifecycle Rule/Checkpoint Cleanup Job/Backup Cleanup/DDL/Migration/测试、设置具体保留期限或执行任何数据删除**。
13. **分层测试策略、SQLite 非权威 Test Double 边界、真实 PostgreSQL 验收范围、Port Contract Parity（Fake Pass ≠ PostgreSQL Adapter Pass）、Sync-only MVP 验收栈、确定性并发测试纪律、DEC-035 Fault Injection 全有或全无、Crash-window 覆盖、Migration Test 真实 Alembic Lineage、PR Required / Scheduled Tier 划分、Flaky Retry-to-green 禁止、Persistence Test Coverage & Fidelity Table 与 Common Persistence Test Harness Qualification 均为项目用户决定（DQ-16 ACCEPTED 2026-08-03），非 PostgreSQL / SQLite / SQLAlchemy / Alembic 官方强制架构。** PostgreSQL 行级 MVCC / SQLSTATE 40001 serialization_failure / 40P01 deadlock_detected / `SELECT FOR UPDATE SKIP LOCKED` / SAVEPOINT / DDL 事务性 = 官方能力；SQLite 全库单写者与 PostgreSQL 并发语义差异大 = 官方事实（[架构推断]：并发语义在 SQLite 与 PostgreSQL 间不可移植）；SQLAlchemy sync/async 两条 stack、`metadata.create_all()` 与 Alembic Migration 的区别 = 官方能力边界；**但 Layered Test Strategy（Pure Domain/Application Unit Tests + Port Contract Parity Tests + Real PostgreSQL Persistence Acceptance Tests + Deterministic Multi-connection Concurrency Tests + Real Migration/Upgrade/Recovery Tests + Crash-window/Fault-injection Tests + Production-topology-specific Qualification）、SQLite = OPTIONAL NON-AUTHORITATIVE TEST DOUBLE（SQLite as Persistence Acceptance Engine = PROHIBITED）、正式持久化语义必须真实 PostgreSQL、Fake Pass ≠ PostgreSQL Adapter Pass、MVP 只测 SQLAlchemy 2.x sync + Psycopg 3 sync（Async Stack 不属 MVP Persistence Acceptance Scope）、钉定版本 + 独立 Test Role + 隔离 Database/Schema + 正式 Alembic Migration + 10 项证据元数据、单连接 SAVEPOINT Rollback Fixture 受限 + 多连接真实 Commit、并发 Actor 独立 Connection/Session/Transaction + 确定性协调（不主要依赖 sleep）、19 类并发场景真实覆盖、DEC-035 Fault Injection（Commit Outcome Unknown 不只靠 Mock commit 抛异常）、9 类 Crash Window、Migration Test 真实 PostgreSQL（metadata.create_all() 不作 Migration Acceptance 唯一 Schema 来源）、Required PR Checks 含 correctness-critical PostgreSQL 测试（correctness-critical Invariant 不只 Nightly）、禁止自动 Retry-to-green（Application Retry Under Test ≠ CI Test Retry）、合成测试数据（精确 Sensitive Test Fixture 规则留 DQ-17，已由 DQ-17 于 2026-08-03 正式决定）、Persistence Test Coverage & Fidelity Table、Common Persistence Test Harness Qualification 均为 Accepted Architecture Decision（项目用户决定），非 PostgreSQL、SQLite、SQLAlchemy 或 Alembic 官方强制架构**；**不得声称 SQLite 能证明 PostgreSQL 并发语义、SQLite Test 是正式 Persistence Acceptance、Fake Pass 替代 PostgreSQL Adapter Pass、所有测试必须使用 PostgreSQL（Candidate B 已拒绝：Pure Domain Unit Test 不使用数据库）、Async Persistence Stack 已被接受、`metadata.create_all()` 等于 Migration Acceptance、自动 Retry 可以解决 Flaky Test、DQ-16 已授权创建测试**；Common Persistence Test Harness Qualification / Test Database 与 Schema 命名 / CI Job 拓扑 / Fixture 策略 / Contract Suite 实现 → 第一个获授权 Persistence Spike（延迟实现决定），Async Stack 兼容性 → 未来新架构与兼容性决定，精确 Sensitive Test Fixture 规则 → DQ-17；**Persistence Test Coverage & Fidelity Table REQUIRED 但 NOT AUTHORIZED；New Independent DQ-16 Technical Spike = NOT REQUIRED（已有五项专项 Spike 继续有效并共享合格真实 PostgreSQL Test Harness 原则）；Common Persistence Test Harness Qualification = REQUIRED AS PART OF FIRST AUTHORIZED PERSISTENCE SPIKE 但 NOT AUTHORIZED，不得借 DQ-16 归档创建 Harness/Container/CI Job/Fixture/Contract Suite/测试代码/基础设施或修改 CI Workflow**。
14. **分类敏感数据保护、Secret-reference-only 持久化、ephemeral adapter-scoped 解析、多平面分类传播、严格 Msgpack Allowlist、Least-privilege Role/Credential/Pool 分离、选择性信封加密、三层加密责任、Redaction-before-persist、跨 Tenant/跨安全域去重禁止、合成测试数据与 Secret Detection、Sensitive Data Matrix 与 Cross-cutting Qualification 均为项目用户决定（DQ-17 ACCEPTED 2026-08-03），非 LangGraph / PostgreSQL / OWASP 或其他外部标准的原生强制实现。** LangGraph 默认宽松反序列化有 RCE 风险、`LANGGRAPH_STRICT_MSGPACK` 与 `allowed_msgpack_modules` 白名单、Secret 会被明文序列化进 checkpoint（`SecretStr.get_secret_value`）、`EncryptedSerializer`/`LANGGRAPH_AES_KEY` 可用 = LangGraph 官方能力/事实；PostgreSQL Role/Grant/RLS/`search_path`/BYPASSRLS/连接与 Pool 机制 = PostgreSQL 官方能力（[架构推断]：Runtime Role 不应 Superuser/Table Owner/BYPASSRLS）；**但 Secret-reference-only Persistence、SECRET ≠ PII ≠ SENSITIVE BUSINESS DATA 分类语义、Credential Profile 可知而 Secret Value 不可得、Confidentiality Level + Handling Tags 词汇表、Role/Pool 拆分清单（Migration Owner/Business Runtime Write/Read-only/Checkpoint/Dispatch-Worker/Purge/Test/Observability）、CROSS-TENANT/CROSS-SECURITY-DOMAIN CONTENT DEDUPLICATION = PROHIBITED FOR MVP、Sensitive Data, Secret & Cryptographic Control Matrix、Cross-cutting Persistence Security Qualification、Conditional Encryption Spike 均为 Accepted Architecture Decision（项目用户决定），非 LangGraph、PostgreSQL、OWASP 或其他外部标准原生强制**；**不得声称 SecretStr 能保证不持久化、Encrypted Checkpoint 允许保存 Secret、Checkpoint 所有 Metadata 都自动加密、Infrastructure At-rest Encryption 足以替代授权、所有字段必须 Field-level Encryption、RLS 替代 Application Authorization、Hash 等于 Anonymization、Secret 从最新 Commit 删除后无需轮换、DQ-17 已实现 Secret Manager、Encryption、Role 或 Redaction**；Secret Manager/KMS/Vault/HSM 产品选择、加密算法、Rotation Period、Key Hierarchy 实现 → 后续 Infrastructure/Deployment 决策（延迟产品/provider 选择），TLS/CA/证书部署配置 → 后续 Infrastructure/Deployment 决策（延迟实现决定）；**Sensitive Data, Secret & Cryptographic Control Matrix REQUIRED 但 NOT AUTHORIZED；New Independent DQ-17 Technical Spike = NOT REQUIRED；Cross-cutting Persistence Security Qualification = REQUIRED IN FIRST AUTHORIZED PERSISTENCE SPIKES 但 NOT AUTHORIZED；Selective Field Encryption & Key Rotation Spike = CONDITIONALLY REQUIRED 但 NOT AUTHORIZED；不得借 DQ-17 归档创建 Secret Manager/KMS/Vault/HSM/Encryption Adapter/Encrypted Column/Key Registry/Rotation Job/PostgreSQL Role/Grant/RLS Policy/Checkpoint Encryption/Redaction Middleware/Security Audit/Secret Scanner/测试/DDL/Migration/Infrastructure**。

---

## 4. 非决策清单（Explicit Non-Decisions of RFC-002）

RFC-002 **显式不**为以下各项做决定（全部移交或排除）：

- LangGraph 生产 Checkpointer 具体实现与 durability/serde 配置 → **RFC-003**
- dispatch backend / Queue / Broker 实现与 relay → **RFC-003**
- API endpoint / HTTP contract / Human Review 提交协议 → **RFC-004**
- Retrieval 实现（chunking/vector/embedding/index）→ **RFC-005**
- LLM provider / prompt / 结构化输出 → **RFC-006**
- 观测栈（logging/metrics/tracing/retry 参数）→ **RFC-007**
- 完整 Event Sourcing → **已由 DEC-013 排除（非 RFC-002 重开）；DQ-11（2026-08-02）正式确认 Candidate B 完整 Event Sourcing REJECTED——项目采用 Authoritative Current Truth + Immutable Business Version Snapshots + Append-only Audit/Transition History + Optional Derived Query Projections，不使用 Event-Sourcing Snapshot（Event Stream 不是 Business Current Truth）**
- 生产部署拓扑 / 容器 / 托管平台 → **后续（超出 RFC-001~007 当前阶段）**
- Secret 管理实现 / 加密密钥管理 → **RFC-002 仅定边界，不实现（DQ-17）**
- 真实 Alembic 迁移脚本 / DB schema DDL → **RFC-002 不创建真实迁移（DQ-14）**

---

> **结论：** RFC-002 的决策权严格限定在「持久化与事务架构」17 个 DQ；所有依赖生产运行时形态（Checkpointer/Queue/API/检索/LLM/观测）的决定全部显式移交，符合 DEC-038 与 RFC-001 的 Acceptance≠Authorization 纪律。
