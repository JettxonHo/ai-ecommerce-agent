# MVP Scope（最小可行产品范围）

> **Status: PARTIAL — 已确认核心任务（DEC-003）、平台范围（DEC-004）、输入分层（DEC-005）、四层输出主结构（DEC-006）、人机协作审核机制（DEC-007）、分级证据可追溯（DEC-008）、阶段级失效与局部重跑（DEC-009）与三维评价框架（DEC-010）；输出字段、引用 UI、工作流与数据技术实现、指标公式与阈值仍待确认**
> 本文件是 Current Truth Layer 的一部分。其内容只能来自用户明确接受的 Decision。
> 当前已确认：MVP 核心任务与交付物（DEC-003）、平台范围与首个演示场景（DEC-004）、输入分层原则（DEC-005）、四层输出主结构（DEC-006）、人机协作审核机制（DEC-007）、分级证据与结论可追溯（DEC-008）、阶段级失效与局部重跑（DEC-009）、三维评价框架（DEC-010）；具体功能条目、各层字段数据类型、输出格式与验收阈值仍未确认，**不得擅自补全**。

---

## 已确认内容（Confirmed）

> 来源：[DEC-003](../decisions/dec-003-product-launch-positioning-and-marketing-brief.md) + [DEC-004](../decisions/dec-004-platform-neutral-core-xiaohongshu-demo.md) + [DEC-005](../decisions/dec-005-layered-mvp-inputs.md) + [DEC-006](../decisions/dec-006-four-layer-structured-marketing-brief.md) + [DEC-007](../decisions/dec-007-single-review-node-and-exception-pauses.md) + [DEC-008](../decisions/dec-008-tiered-evidence-and-traceable-conclusions.md) + [DEC-009](../decisions/dec-009-stage-level-invalidation-and-partial-rerun.md) + [DEC-010](../decisions/dec-010-three-dimensional-mvp-evaluation-framework.md)（Accepted，2026-07-27）

- **MVP 核心任务：** 商品上新（或正式内容推广前）的定位分析 + 结构化营销 Brief 生成，服务中小电商商家的商品运营与内容运营人员（DEC-002）。
- **核心交付物：** 结构化商品上新营销 Brief。
- **基础任务闭环（方向）：** 提交资料 → 检查完整性 → 提取事实与卖点 → 分析目标用户 → 分析需求 / 动机 / 阻碍 → 形成定位与差异化 → 形成内容方向与营销策略 → 生成 Brief → 质量检查与依据展示。
- **平台范围：** 核心能力**平台中立**；**小红书商品种草** 为首个 MVP 演示场景（DEC-004）。
- **产品逻辑边界：** 通用层（通用定位 + 通用 Brief）为核心；平台适配层将通用 Brief 映射为平台表达，首个适配＝**小红书种草 Brief 模板**。
- **输入分层（DEC-005）：**
  - **最低可运行输入：** 商品名称、品类、功能 / 参数 / 文字说明、价格或区间、商家主要卖点、商品图片或展示资料（图片或文字）、本次推广目标。仅凭此层即可运行基础流程。
  - **推荐增强输入：** 用户评论、用户调研、常见用户问题、商品详情页、已有营销内容、品牌定位 / 资料。
  - **可选扩展输入：** 竞品资料、行业报告、平台案例、运营知识文档、历史推广结果、其他。
  - 缺少增强 / 可选输入**不得阻断**基础流程；资料不足须诚实表达。
- **输出主结构（DEC-006）：** MVP 核心输出采用 **事实层 → 洞察层 → 策略层 → 执行层** 四层结构。系统先形成可检查、可追溯的商品分析与营销决策依据，再形成供运营人员使用的执行 Brief；后层内容应能追溯前层依据，**不得**「输入少量信息 → 无依据生成完整营销文案」。**完整小红书标题与正文暂不属于 MVP 核心交付物**（小红书种草 Brief 是执行层的一种平台映射）。
- **人机协作（DEC-007）：** MVP 采用 Human-in-the-loop——**一个常规强制审核节点**（分析草稿生成后、最终 Brief 前）+ **异常暂停与追问**（资料矛盾 / 关键缺失 / 高风险表达时）+ **用户保留最终业务判断权**。常规流程**不要求**每层分别确认；关键事实冲突时系统**不得**自行猜测后继续。完全自动生成、每层分别确认两个方案**未采用但保留为备选**（非永久禁止）。
- **输出可靠性（DEC-008）：** MVP 采用**五类结论标记**（明确事实 / 有证据洞察 / 模型推断 / 待验证假设 / 资料不足）。**事实可追溯**；事实与推断分离；重要洞察保留依据；资料不足时**不得编造**（禁止伪造文档名 / 评论原文 / 引用编号 / 来源）；**用户修改上游事实或关键洞察后，下游依赖内容须重新处理或标记失效**。只展示最终结论、强制逐条原文引用两个方案**未采用但保留为备选**（非永久禁止）。
- **失效与重跑（DEC-009）：** MVP 按**阶段级依赖**（事实 → 洞察 → 策略 → 执行）处理失效与重跑：改事实层 → 洞察 / 策略 / 执行失效重生成；改洞察层 → 策略 / 执行失效；改策略层 → 执行失效；直接编辑执行层 Brief 默认不触发上游重跑。重要业务修改（价格 / 参数 / 功能 / 目标用户 / 定位等）触发下游失效，纯文字修改（错别字 / 标点 / 润色）可不触发。失效内容不得显示为有效 / 进入最终 Brief / 作后续依据。**字段级依赖图暂缓到后续版本**；全量重跑、只改直接字段不更新下游两个方案**未采用但保留为备选**（非永久禁止）。
- **评价框架（DEC-010）：** MVP 用**任务质量 / 结果可靠性 / 用户效率**三维评价，**不**把流畅度或销量作为唯一标准。优先六项指标：事实来源可追溯率、无依据事实数量、四层 Brief 完整率、关键结论人工接受率、生成可用 Brief 任务完成时间、下游失效正确率。销量 / 点击 / 转化 / 互动为未来真实试点业务指标，**非** MVP 唯一验收依据。
- **功能准入原则：** 与「更准确、更高效地完成商品上新定位和营销 Brief」无直接关系的能力，默认不进入 MVP。

> 注：以上为业务任务方向、平台边界、输入分层原则、输出主结构、人机协作原则、可靠性原则、失效重跑规则与评价框架；**不代表**每一步的具体 Agent、工作流与数据技术实现、引用 UI、审核页面、各层最终字段、指标公式与阈值已确定。可靠性原则已成为后续架构与开源仓库选型的硬约束。

---

## In Scope（当前确认的方向，细节待定）

- 通用商品上新前的定位分析（方向）。
- 通用结构化营销 Brief 生成（方向）。
- **四层输出主结构（方向）：** 事实层 → 洞察层 → 策略层 → 执行层；后层可追溯前层（DEC-006）。
- **人机协作（方向）：** 单一关键审核节点（草稿后、最终 Brief 前）+ 异常暂停 + 用户最终判断权（DEC-007）。
- **输出可靠性（方向）：** 五类结论标记 + 事实可追溯 + 重要洞察保留依据 + 资料不足不得编造 + 修改产生依赖失效（DEC-008）。
- **阶段级失效与局部重跑（方向）：** 事实→洞察→策略→执行阶段级依赖失效 + 重要/非重要修改区分 + 重跑后复核；字段级依赖图暂缓（DEC-009）。
- **三维评价（方向）：** 任务质量 / 可靠性 / 用户效率 + 六项优先指标；不以流畅度或销量为唯一标准（DEC-010）。
- 平台适配层（方向）：将通用 Brief 映射为平台表达。
- **首个平台演示场景：小红书商品种草 Brief 模板**（方向）。
- **输入分层（方向）：** 最低可运行输入 + 推荐增强 + 可选扩展；仅最低输入即可运行。
- 服务对象：商品运营与内容运营人员（DEC-002）。

> 具体功能条目、通用 Brief 各层字段 / 必填规则 / 输出格式、小红书模板字段、各输入字段的数据类型 / 是否强制 / 文件格式、输入完整度机制的具体形态、审核页面与异常判断规则，**均待后续讨论确认**，不得在本次归档中擅自补全。

---

## Out of Scope（明确不进入当前 MVP，可在未来扩展）

> 以下能力**不进入当前 MVP**，**不代表永久拒绝**，可作为后续扩展参考：

- 竞品自动监控；
- 多平台内容批量生成；
- 自动发布内容；
- 广告投放；
- 销售预测；
- 库存分析；
- 客服；
- 完整店铺经营诊断。

> 另：以下**尚未确认**（既未纳入也未排除，待讨论）——
> - 输入相关：每个字段的具体数据类型、最低输入字段是否全部强制、商品图片是否技术必填、支持哪些文件格式、是否支持商品链接、是否自动抓取商品页面 / 评论 / 竞品数据、是否使用联网搜索、是否写入长期知识库、是否使用向量数据库、文档切分与检索方式、输入完整度评分算法、信息不足时具体询问内容、隐私 / 权限 / 数据保存策略。
> - 输出相关：四层结构各层的最终字段 / 必填规则、输出格式（Markdown / JSON / 表格 / 其他）、引用显示形式、置信度计算方式、用户能否单独编辑某一层、人工审核节点、小红书模板字段、是否将完整小红书笔记 / 标题 / 正文 / 标签 / 配图作为附加能力（DEC-006 已确认其**非 MVP 核心交付物**）、官方创作指引、抓取小红书公开内容 / 爆款分析、小红书 API。
> - 平台相关：是否支持淘宝 / 抖音等其他平台；平台适配层技术实现形式；是否独立平台 Agent / Skill。
> - 协作 / 工作流相关：是否采用 LangGraph / Interrupt API、是否数据库级持久化、Checkpoint 实现方式、用户修改后重跑全部还是局部流程、审核页面结构、草稿编辑粒度、异常判断规则、高风险表达范围、多人协作审核、版本历史、能否跳过常规审核节点（DEC-007 仅确认协作原则，技术实现均未确认）。
> - 可靠性 / 可追溯相关：引用 UI、是否显示置信度数值、置信度算法、来源 ID 与数据结构、是否使用 RAG / 向量数据库 / 网页检索 / 知识图谱、是否保存原始文档片段、外部来源可信度评估规则（DEC-008 仅确认可靠性原则与五类标记，技术实现均未确认）。
> - 失效 / 重跑相关：是否采用 LangGraph / Checkpoint / Interrupt、工作流节点划分、状态保存数据库、失效状态数据结构、如何自动识别重要修改、是否保留历史版本、是否显示修改前后差异、自动重跑或手动触发、是否强制二次审核、字段级依赖图是否进入后续版本（DEC-009 仅确认阶段级失效规则，技术实现均未确认）。
> - 评价相关：每项指标公式、目标阈值、测试数据集、用户测试人数、人工评分标准、是否使用 LLM-as-a-Judge、埋点方案、对照组设计、测试环境、是否接入真实业务数据、是否展示评测 Dashboard（DEC-010 仅确认三维框架与六项优先指标，具体方式均未确认）。

---

## 文档骨架（占位，内容待填充）

> 以下章节标题仅作为未来结构占位，**除已标注外当前为空**，不构成任何范围声明。

- MVP 目标 —— 核心任务 + 平台范围 + 输入分层 + 四层输出主结构 + 人机协作 + 分级证据可追溯 + 阶段级失效重跑 + 三维评价方向已确认；量化目标待确认（见 Session-001 Question-003）
- 关键假设与依赖 —— 待确认
- 验收标准 —— **框架层已确认**（DEC-010 三维评价 + 六项优先指标）；具体公式 / 阈值 / 测试方式待确认（不得擅自补全）
- 风险 —— 见 Session-001 Risk-001 / 002 / 003

---

## 当前状态

- 项目处于 **Product Discovery 阶段**。
- 已确认 MVP 核心任务与交付物（DEC-003）、平台范围与首个演示场景（DEC-004）、输入分层原则（DEC-005）、四层输出主结构（DEC-006）、人机协作审核机制（DEC-007）、分级证据与结论可追溯（DEC-008）、阶段级失效与局部重跑（DEC-009）、三维评价框架（DEC-010）；输出字段、引用 UI、工作流与数据技术实现、指标公式与阈值仍待确认。
- 其余具体内容，必须等到对应 Proposed Decision 被用户明确接受并记为 Accepted Decision（见 [../decisions/](../decisions/)）后，才能写入。

---

## 待讨论的开放问题（MVP 相关）

- MVP 的核心目标：**核心任务已确认**（DEC-003）；量化评价目标见 Session-001 Question-003（待确认）。
- 哪些功能必须在 MVP 内？哪些明确排除？—— 核心任务 + 平台范围 + 输入分层方向已确认；具体功能条目与字段待确认（见 In / Out of Scope）。
- 平台范围：**已确认**（DEC-004）；小红书模板字段与其他平台支持待确认。
- 输入设计：**分层原则已确认**（DEC-005）；字段类型 / 格式 / 采集方式 / 完整度机制待确认。
- 通用 Brief 的输出结构：**四层主结构已确认**（DEC-006：事实 / 洞察 / 策略 / 执行）；各层字段 / 必填 / 输出格式 / 引用 / 置信度仍待确认。完整小红书笔记非核心交付物（DEC-006），是否作为附加能力待确认。
- 人机协作 / 审核节点：**原则已确认**（DEC-007：单一关键审核节点 + 异常暂停 + 用户最终判断权）；LangGraph / Interrupt / Checkpoint / 重跑策略 / 审核页面 / 异常规则仍待确认。
- 输出可靠性 / 可追溯：**原则已确认**（DEC-008：五类结论标记 + 事实可追溯 + 修改产生依赖失效）；引用 UI / 置信度算法 / 来源数据结构 / RAG / 向量库 / 知识图谱仍待确认。
- 失效与局部重跑：**规则已确认**（DEC-009：阶段级失效 + 重要 / 非重要修改区分）；LangGraph / Checkpoint / 自动重跑交互 / 版本历史 / 字段级依赖图待确认。
- 评价框架：**框架已确认**（DEC-010：三维评价 + 六项优先指标）；指标公式 / 阈值 / 测试集 / 人数 / 埋点 / Dashboard 待确认。
- 是否需要 RAG？MVP 内 RAG 的最小形态？—— **尚未确认**。
- 是否需要 Skill 机制？MVP 内包含哪些 Skill？—— **尚未确认**。
- 是否基于某个开源项目改造？改造范围？—— **尚未确认**。
- MVP 的验收标准？—— **尚未确认**。

讨论与提案记录在 [../sessions/](../sessions/)；确认后的决定记录在 [../decisions/](../decisions/) 并同步回本文件。

---

## 同步规则

- 仅在决定被明确接受后更新本文件。
- MVP Scope 必须与 [prd.md](prd.md) 一致；**不得擅自扩大范围**（Out of Scope 项不得自动纳入）。
- 不得为使文档「完整」而补充未经讨论的功能或 Brief / 模板字段或输入技术细节。
- 冲突时按 [../governance/documentation-rules.md](../governance/documentation-rules.md) 第 6 节优先级裁决。
