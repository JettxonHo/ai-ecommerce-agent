# User Flows（用户流程）

> **Status: PARTIAL — 已确认高层级任务闭环（DEC-003）、平台映射（DEC-004）、输入分层流程（DEC-005）、四层输出流程（DEC-006）、人机协作审核流程（DEC-007）、分级证据可追溯（DEC-008）与阶段级失效局部重跑（DEC-009）；具体步骤、审核页面、引用 UI、异常规则、重跑交互与工作流技术实现仍待讨论**
> 本文件是 Current Truth Layer 的一部分。其内容只能来自用户明确接受的 Decision。
> 当前已确认：MVP 核心任务的高层级流程（DEC-003）、到首个演示场景的映射（DEC-004）、输入分层的高层流程（DEC-005）、四层输出主结构（DEC-006）、单一关键审核节点 + 异常暂停的人机协作流程（DEC-007）、分级证据与结论可追溯（DEC-008）、阶段级失效与局部重跑（DEC-009）；具体步骤、交互页面、引用 UI、异常判断规则、重跑交互与工作流技术实现仍未确认。
> **DEC-041 同步：** 交互形态为引导式任务工作台；允许结构化表单、文本、TXT / Markdown、文本型 PDF 与评论 CSV。不提供图片 / OCR、链接抓取、主动联网研究、完整小红书正文生成或自动发布流程。
> **DEC-042 同步：** 工作台必须让用户无需理解内部实现即可完成闭环，并能理解、审核和追溯主要结论；具体页面和交互仍待冻结。

---

## 已确认内容（Confirmed）

> 来源：[DEC-003](../decisions/dec-003-product-launch-positioning-and-marketing-brief.md) + [DEC-004](../decisions/dec-004-platform-neutral-core-xiaohongshu-demo.md) + [DEC-005](../decisions/dec-005-layered-mvp-inputs.md) + [DEC-006](../decisions/dec-006-four-layer-structured-marketing-brief.md) + [DEC-007](../decisions/dec-007-single-review-node-and-exception-pauses.md)（Accepted，2026-07-27）

- **高层级流程（已确认）：**

```
提交资料
        ↓
输入检查（资料完整性与冲突检查；提示可选增强资料）
        ↓
生成分析草稿（事实层 → 洞察层 → 初步策略层）
        ↓
用户审核与修改（单一关键审核节点）
        ↓
用户确认
        ↓
生成最终营销 Brief（执行层 + 平台模板映射；首个演示＝小红书种草 Brief）
        ↓
最终编辑与导出
```

  - **输入检查（DEC-005）：** 仅最低可运行输入（商品基础资料 + 推广目标）即可继续，不阻断；系统提示可选增强资料（评论 / 品牌 / 已有内容 / 竞品 / 行业等），用户可选择补充或直接继续。
  - **生成分析草稿（DEC-006）：** 按四层结构逐层形成，后层可追溯前层依据——事实层（**不得混入未标记推断**）→ 洞察层（区分「有证据 / 有限推断 / 待补」）→ 初步策略层（定位 / 价值主张 / 差异化 / 卖点优先级 / 传播角度）。
  - **用户审核与修改（DEC-007，单一关键审核节点）：** 在草稿生成后、最终 Brief 生成前，用户可修正事实、补充资料、删改洞察、接受 / 否定假设、调整目标用户 / 卖点优先级 / 商品定位、标记不可公开信息、选择继续或退回重分析；**不要求**每层分别确认。用户保留最终业务判断权。
  - **生成最终营销 Brief（DEC-003 / 004 / 006）：** 用户确认后生成执行层 Brief，并将通用 Brief 映射为平台展示模板；**首个演示场景＝小红书种草 Brief**（完整小红书标题 / 正文非核心交付物）。
  - **最终编辑与导出：** 用户查看、编辑并导出最终结果。

> **异常分支（DEC-007）：** 检测到关键冲突或缺失（资料矛盾 / 关键参数缺失 / 卖点缺依据 / 无法判断目标用户 / 事实与推断难区分 / 高风险表达等）时：

```
检测到关键冲突或缺失
        ↓
暂停工作流
        ↓
说明问题、指出冲突 / 缺失、提出补充问题
        ↓
用户补充
        ↓
继续或重新分析相关步骤
```

> 关键事实冲突时系统**不得**自行猜测后继续生成最终 Brief。

> **证据标记与可追溯（DEC-008）：** 分析草稿与最终 Brief 中的内容须标注五类类型（明确事实 / 有证据洞察 / 模型推断 / 待验证假设 / 资料不足）；明确事实须可追溯到用户输入 / 资料 / 已确认来源 / 审核修正；重要结论须保留与主要上游依据的关系。**用户在审核节点修改某项事实或关键洞察后，依赖该内容的下游策略与执行 Brief 不再被视为有效**——需重新生成 / 标记失效 / 提示更新（与 DEC-007 的「修改后重跑」同源；具体重跑与展示机制未定）。

> **阶段级失效与局部重跑分支（DEC-009）：** MVP 按阶段级依赖（非字段级依赖图）处理用户修改后的失效与重跑。重要业务修改触发下游失效，纯文字修改可不触发（自动区分方式未定）；失效内容不得显示为有效 / 进入最终 Brief / 作后续依据。

```
用户修改事实层（价格 / 参数 / 功能 / 卖点等）
        ↓
下游阶段标记失效（洞察 / 策略 / 执行层）
        ↓
重新生成洞察、策略与执行层
        ↓
用户重新查看受影响内容（进入 / 重新进入审核节点）
```

```
用户修改洞察层（目标用户 / 需求 / 动机 / 阻碍 / 场景等）
        ↓
策略与执行层标记失效（事实层不变）
        ↓
局部重新生成策略与执行层
        ↓
用户重新查看受影响内容
```

```
用户修改策略层（定位 / 价值主张 / 卖点优先级 / 差异化 / 传播方向）
        ↓
执行层标记失效（事实 / 洞察层不变）
        ↓
重新生成最终 Brief
        ↓
用户重新查看受影响内容
```

> 直接编辑**执行层**最终 Brief：保存用户编辑，**默认不触发上游重跑**（视为最终业务调整）。上游重要修改采用阶段级局部重跑；自动或手动触发、AI 版与用户编辑版的差异展示、重要修改识别和再次确认交互仍待策划。

> 该流程通过引导式任务工作台承载；聊天记录不作为业务状态。小红书输出是通用 Brief 的一种适配方式，**不是**唯一输出形态。四层最终字段、引用 UI、审核页面 / 编辑粒度、进度与错误呈现仍待产品策划；暂停 / 恢复 / Checkpoint 的生产实现由 RFC-003 / 004 决定。

---

## 待讨论（具体化，尚未确认）

> 以下均**尚未确认**，列为后续流程设计的开放项：

- 具体步骤的拆分与顺序；
- 交互页面、表单与入口；
- 资料上传的具体交互（允许格式已冻结；拖放 / 选择器、大小与数量限制、解析进度和错误提示仍未确认）；
- 输入完整度检查的具体表现（缺失字段提示 / 完整度说明 / 补充问题 / 置信度 / 证据不足标记——形式均未确定）；
- 人工审核 / 审阅 / 修改节点：**节点存在、位置与不可跳过已确认**；编辑界面、字段粒度、交互控件与版本历史尚未确认，多人协作不进入受控单工作区首个 Goal；
- 异常与回退路径：**异常暂停 + 追问已确认**（DEC-007）；异常判断规则、高风险表达具体范围尚未确认；
- 阶段级失效与局部重跑：**失效范围与局部重跑规则已确认**；自动或手动触发、重要修改识别、再次确认、版本历史 / 修改前后差异仍待策划，字段级依赖图不进入首个 Goal；
- Retrieval / Evidence 与 4 个 Core Skills 的概念边界已确认；各步骤的最终触发、进度映射和异常交互仍待产品规格与 RFC-003～006 明确；
- 通用 Brief 与小红书种草 Brief 的展示与导出形式；
- 四层结构各层的字段 / 必填规则 / 输出格式（Markdown / JSON / 表格 / 其他）、引用显示形式、置信度计算方式；
- 证据标记与可追溯的具体呈现（DEC-008）：**五类标记 + 可追溯原则已确认**；引用 UI（来源标签 / 引用卡片 / 抽屉详情 / 证据列表 / 结论类型图标 / 置信度提示）、是否显示置信度数值、来源数据结构、修改后失效与重跑的流程表现均未确认；
- 各层查看 / 编辑控件与可编辑字段；人工审核位置与单一性已确认，不再作为开放问题；
- Xiaohongshu Brief 的最终字段、展示与导出；完整小红书正文、图片 / 视频生成和自动发布不进入首个 Goal；
- Xiaohongshu Adapter 的映射链已确认；触发时机、进度与失败呈现仍待交互和 API 契约冻结。

---

## 文档骨架（占位，内容待填充）

> 以下章节标题仅作为未来结构占位，**除已标注外当前为空**，不构成任何流程声明。

- 核心场景列表 —— 核心场景方向已确认（DEC-003 / 004 / 005 / 006 / 007 / 008 / 009）；细分场景待确认
- 关键流程（触发条件、步骤、分支、异常、成功标准）—— 高层流程 + 审核节点 + 异常分支 + 证据可追溯 + 阶段级失效重跑 + 行为型演示成功边界已确认；细节待讨论
- 与 Agent / Retrieval / Skill 的交互点 —— 概念职责已确认；页面状态、运行进度与 RFC-003～006 实现映射待细化

---

## 当前状态

- 项目处于 **Pre-development Planning（正式开发前策划）阶段**；业务实现与长期 Goal 均未启动。
- 已确认高层任务流程、平台映射、输入分层、四层输出、Human Review、证据追溯、阶段级失效重跑和行为型演示成功边界（DEC-003～009 / DEC-042）；具体审核页面、引用 UI、重跑交互、异常规则与工作流实现仍待讨论。
- 其余具体流程，必须等到对应 Proposed Decision 被用户明确接受并记为 Accepted Decision（见 [../decisions/](../decisions/)）后，才能写入。

---

## 待讨论的开放问题（Flows 相关）

- 用户与 Agent 交互的核心入口是什么？—— **已确认**为引导式任务工作台；具体页面与导航结构待策划。
- 关键业务流程的细节？—— 高层已确认（DEC-003 / 004 / 005 / 006 / 007 / 008 / 009）；细节待讨论。
- 输入不足时如何与用户交互（提示 / 补充问题 / 置信度）？—— **异常暂停 + 追问已确认**（DEC-007）；提示形式 / 置信度 / 异常判断规则尚未确认。
- 四层输出的各层字段 / 必填 / 输出格式？—— **尚未确认**（DEC-006 仅确认四层主结构与追溯原则）。
- 证据标记与可追溯如何呈现？—— **五类标记 + 可追溯原则已确认**（DEC-008）；引用 UI / 置信度数值 / 来源数据结构 / 失效与重跑表现尚未确认。
- 审核节点：位置、单一性与不可跳过已确认；编辑界面 / 字段粒度 / 版本历史待策划，多人协作不进入受控单工作区首个 Goal。
- 通用 Brief 到小红书 Brief 的 Adapter 映射链已确认；具体触发、进度、失败和结果呈现待策划。
- Direct-first、按需混合 Retrieval 与 4 个 Core Skills 的职责已确认；具体节点触发、Provider、索引和进度映射待 RFC-003 / 005 / 006。
- 异常与回退路径如何设计？—— **异常暂停 + 追问已确认**（DEC-007）；异常规则、高风险表达范围、工作流技术实现尚未确认。
- 局部重跑如何设计？—— **阶段级失效范围与局部重跑规则已确认**；自动或手动触发、再次确认、重要修改识别、版本历史 / 差异呈现待策划，字段级依赖图不进入首个 Goal。

这些问题的细化依赖于 [user-personas.md](user-personas.md) 与 [../agents/](../agents/) 的明确。

讨论与提案记录在 [../sessions/](../sessions/)；确认后的决定记录在 [../decisions/](../decisions/) 并同步回本文件。

---

## 同步规则

- 仅在决定被明确接受后更新本文件。
- Flows 必须与 [user-personas.md](user-personas.md)、[prd.md](prd.md)、Agent Specs 保持一致。
- 不得为使文档「完整」而补充未经讨论的流程（具体步骤 / 页面 / 异常分支 / 输入交互 / 小红书模板字段不得臆造）。
- 冲突时按 [../governance/documentation-rules.md](../governance/documentation-rules.md) 第 6 节优先级裁决。
